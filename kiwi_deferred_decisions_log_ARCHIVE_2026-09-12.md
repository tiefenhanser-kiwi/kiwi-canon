<!-- ============================================================
     MIRROR COPY OF AN ARCHIVE (NOT project knowledge) — generated 2026-09-12 11:25Z (UTC) by chat-Claude from Claude project knowledge.
     Source of truth is project knowledge. This file is a READ-ONLY snapshot.

     ⚠️ QUOTE THE TIMESTAMP ABOVE BEFORE YOU QUOTE ANYTHING ELSE FROM THIS FILE.
     On 2026-09-04 a lane reported a freshly-minted entry "is not written" because this
     mirror was two days and six entries stale. It was right about the mirror and wrong
     about canon. Staleness must surface as a reported fact, never as a silent premise.

     NEVER read from this mirror:
       - current block / current position / HEAD
       - next-available ID counters (D-WS7, D-WS9, BUG)
     Those come from your PROMPT ONLY. Deriving a counter by grepping this file — headings
     or pointer line — is exactly the banned move, and it has already been made once.

     kiwi_remediation_progress.md is DELIBERATELY ABSENT: it is the single source of
     position (§A), and a second copy on disk is a second claimant by construction.
     design_tokens_4.ts is DELIBERATELY ABSENT: it has drifted from the repo;
     artifacts/kiwi/constants/tokens.ts is the only truth for tokens.

     If a ruling in this file contradicts your prompt: SAY SO. Do not silently pick one.
     ============================================================ -->

# Kiwi — Deferred Decisions Log ARCHIVE — September 12, 2026

**What this is.** The D-WS9 pointer line's dated narration chain (30 KB) removed VERBATIM from `kiwi_deferred_decisions_log.md` on September 12, 2026 (§24.9). **No decision entry moved — every `### D-WS*` heading stayed in the live log.** Archived DOWN TO THE MIRROR (§24.10). NOT project knowledge; NOT a source of position or counters.

---

## The D-WS9 pointer line as it stood (September 12, 2026)

**Next available D-WS9 ID:** D-WS9-239 (updated **September 12, 2026 — D-WS9-238 minted from Hans's challenge to the BUG-255 ruling: does the shelf guard relax when the eligible shelf is thin? Not ruled; costs owed.**) *Prior:* D-WS9-238 (updated **September 10, 2026, evening ET — D-WS9-237 minted and REWRITTEN the same evening: the meal-selection plan flow (the wizard returns meals to multi-select; the Kiwi Cookbook is searchable to add meals; suggested plans stay). ⚠️ Its first draft said "not in canon" — FALSE, chat-Claude searched the wrong words; the pieces were under Kiwi Cookbook (D-WS9-120), Playlist (D-WS9-234) and D-WS9-200. D-WS9-234 gained Hans's September 10 description of the Playlist.** *Prior:* **September 10, 2026 (late) — D-WS9-235 RULED by Hans: derive meal time from steps WITH THE SCHEDULER'S PARALLELISM, capture ACTIVE and TOTAL time, overnight cached-prompt tagging if the step data needs it, and an unmeetable cap gets an honest number rather than an error page. D-WS9-236 minted: filtering the wizard on active time is NOT MVP — roadmap.** *Prior:* **September 10, 2026 — D-WS9-235 minted; ruling owed.** *Prior:* **September 8, 2026 — D-WS9-232 · 233 · 234 minted from Hans's product notes.** 🔴 **D-WS9-234 is the one to read before touching WS7-11: the "playlist" RENAMES what D-WS7-202 called a favorite, and it ANSWERS that entry's blocking open question.** ✅ **D-WS9-233's scope fork is RULED (September 8): PER-DINNER-SERVING targets that steer generation, displayed as asterisked daily averages. Store the target WITH its scope — "daily average" changes meaning the day breakfast/lunch land.** ⚠️ **D-WS9-232's removal sweep must check schema DEFAULTS, not just screens — see `wantsLeftovers`.** *Prior:* **September 7, 2026 (auth+email Phase 0) — D-WS9-231 minted: `kiwi://` custom-scheme links do not survive email, and BOTH mail-bearing flows mint one.** 🔴 **IT GATES BUG-235's CLIENT BUILD RATHER THAN FOLLOWING IT — the deep-link target, the token hand-off and the fallback all differ by option, so choosing after the build means building twice.** ⚠️ **The existing `kiwi://verify-email` success proves the ROUTER is wired and says nothing about whether a mail client will render the link.** Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps.) *Prior:* D-WS9-231 (updated **September 7, 2026 (device pass on `fa1859c`) — D-WS9-230 minted and RULED: NO BACKFILLS. Fixes are FORWARD-ONLY and Hans's existing plans/lists/meals are disposable.** 🔴 **A LARGE PERMANENT SCOPE REDUCTION — this project has repeatedly built a second thing to repair what a bug already wrote, in service of one developer's throwaway test corpus.** 🔴 **BUT THE CARVE-OUT IS NOT OPTIONAL: the SHARED INGREDIENT CATALOG is product data every new user inherits and is NOT covered — D-WS9-226's CSV load stays in scope. The test is "would a user signing up tomorrow inherit this row?"** Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps.) *Prior:* D-WS9-230 (updated **September 7, 2026 (WS9A Phase 1 close) — D-WS9-229 minted: the grocery-generate limiter's BUDGET is reasoned from a ladder, not priced, and CC said so itself.** 🔴 **ITS FIRST VICTIM IS HANS — 429 after four generates, then one per 75 seconds, which reads as a defect during a device pass.** ⚠️ **Three questions, not one: the beta budget, whether a dev escape hatch exists (and it must be impossible to enable in production), and what the user sees at the limit — today a bare 429 during a generating overlay, which is a hang.** ✅ **The `userId` key is settled and should not be reopened.** Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps.) *Prior:* D-WS9-229 (updated **September 7, 2026 — D-WS9-228 minted and RULED: a recurring staple's default names the GRADE the recipes assume — eggs default to `1 dozen Large`.** 🔴 **THE SALES-SHARE FIGURE HANS ASKED FOR WAS SEARCHED FOR AND DOES NOT EXIST IN A CITABLE FORM — the ruling rests on the American recipe convention instead, which is the stronger argument because it ties the default to the app's own recipe math.** ⚠️ **IT IS AN ALIAS-PRECEDENCE CHANGE ONLY: the `egg` / `Large eggs` `distinct` edge STANDS and no fold is authorised.** Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps.) *Prior:* D-WS9-228 (updated **September 7, 2026 — D-WS9-227 minted: a shopper can change how much, never what. Hans's device observation, measured against the route contract rather than the screen.** Earlier the same day (later) — 🔴 D-WS9-224 IS RULED: Hans supplied the missing DIRECTION mechanism — fold to the GENERIC name and carry the specific as a QUALIFIER (*"2 bell peppers (at least one red)"*). No new ID minted. Earlier the same day — the read-only 4-list audit: D-WS9-225 (pack yield is DATA, not a garlic-only constant — the subUnit ladder exists for 1 of 1,569 rows) and D-WS9-226 (ingredient INTAKE: a new row arrives computable or flagged, never with an AI pack sized to one plan's need) minted; D-WS9-207 carries its audit answer.** *Prior:* **September 6, 2026 (A3 close) — **D-WS9-224: the subsumes fold needs a DIRECTION mechanism; six ruled folds are held because the reader keys on the shortest name and would strip a requirement.** ✅ **The component pool is LIVE — BUG-200/207/208/211 closed, 3,186 → 3,073 rows, zero under-buys.** 🔴 **Cilantro is still three rows (BUG-213) and needs one catalog figure.** ⚠️ **New durable rule from A3: a correct food classification is not automatically a safe fold — parsley is the counterexample.**) *Prior:* **September 6, 2026 (A3-prep close) — **D-WS9-223 is Hans's SUBSUMES DISCRIMINATOR: does the distinction change the dish?** ✅ **It reconciles his salt ruling and his romaine ruling, which had pointed opposite ways on the same-looking question.** ⚠️ **The AXIS is ruled; the per-item partition is NOT — test each of the 18 against it and report before applying.** ⚠️ **D-WS9-218 is now 🟠 CONTESTED with its `[BLOCKER]` tag removed: a second CC session reports the basis unit IS stored and a `fromUnit` column would unblock ZERO edges.**) *Prior:* **September 6, 2026 (A2b close) — **D-WS9-222 names the data gap that blocks D-WS9-221 clause 2: there is exactly ONE pack per ingredient and ZERO of 1,569 rows hold a SET of retail sizes.** ⚠️ **A2b ships clause 1 only — it ranks the packs a merge group already holds and cannot propose a size the data does not contain.**) *Prior:* **September 6, 2026 — **D-WS9-221 is Hans's PURCHASE-PACK RULING: the buy line must always name a size, and the pack is the smallest normal size that retires the SUMMED demand.** ⚠️ **It re-rules BUG-124 from the product side, carves pantry staples out to BUG-171's need-not-pack rule, and carries ONE existence question that gates any build — whether a SET of retail sizes is stored anywhere, or only one pack per `Ingredient` row.**) *Prior:* **September 5, 2026 (A2 Phase 1) — **D-WS9-220 is Hans's GARLIC ARITHMETIC SPEC, and it resolves the `subUnit`-vs-relation "duplicate mechanism" question by showing they are two HALVES of one answer rather than two answers.** ⚠️ **It also establishes the general shape for every component pool: sum in the child unit, convert, `ceil` to whole parents, and render the PARENT on the buy line.**) *Prior:* **September 5, 2026 (A2 Phase 0) — 🔴 **D-WS9-218 IS A BLOCKER FOUND BY CC AGAINST ITS OWN BRIEF: the COMPONENT yield's basis unit is not stored anywhere, and `Ingredient.defaultUnit` cannot supply it — 57 of 95 edges are arithmetically undefined.** **D-WS9-219 names the structural boundary that has now stalled FOUR separate things: `IngredientAlias` is string→row, `IngredientRelation` is row→row, and a free-text name with no catalog row is reachable by neither.**) *Prior:* **September 5, 2026 (later) — **D-WS9-217 rules the SUBSUMES reader's direction: purchase de-duplication, never substitution.** ⚠️ **Its consequence is that the 556 `subsumes` rows are NOT uniformly safe and need a second classification pass — the ruling creates work rather than closing it.** *Prior:* **September 5, 2026 — 🔴 **D-WS9-216 RULES THE ALLERGEN STAMPING TEST AND RETIRES THE "how confident are we" FRAMING: stamp what is INHERENT to a product, never what is POSSIBLE in a brand of it.** Hans-ruled; it collapses a proposed 110-name review to roughly a dozen items and makes the AI tier a worksheet rather than an authority.) *Prior:* **September 4, 2026 (device pass) — D-WS9-213, -214, -215 minted from Hans's run. 🔴 **D-WS9-214 IS THE BIG ONE: the allergen vocabulary fails on PROCESSED PRODUCTS whose names contain no allergen word — mayonnaise, pizza dough, brioche, beer — and PIZZA IS ON A GLUTEN-FREE SHELF RIGHT NOW. By meal count that class is larger than everything Lane A fixed, and it forces a mechanism decision rather than another list patch.**) Prior: D-WS9-213 (updated **September 4, 2026 (later) — D-WS9-212 minted: 🔴 **CC CANNOT READ PROJECT KNOWLEDGE, AND THE ONLY CANON IT CAN REACH WAS 6 ENTRIES STALE.** A prompt told CC to *read* a freshly-minted entry; CC correctly reported it did not exist. **Inline rulings into prompts — never cite a path the reader cannot reach.**) Prior: D-WS9-212 (updated **September 4, 2026 — the allergen-vocabulary audit. D-WS9-211 minted: the stamp vocabulary is wrong in FOUR ways and TWO of them let meals through a filter today — a farro dish passes Gluten-free and a crawfish dish passes Shellfish-free, both confirmed against live data. ⚠️ **D-WS9-077's headline number is stale by 5× and its "not a safety issue" line rests on a rationale that expired** — see D-WS9-211. ⚠️ **D-WS9-206 was AMENDED the same day with Hans's placement ruling; no ID was minted for that amendment.** **BUG-201 handed into the Lane A prompt as reserved — VOID-stub it at close if unused.**) Prior: D-WS9-211 (updated **September 3, 2026 — WS9 contrast/legibility arc close. D-WS9-206 through D-WS9-210 minted: the two-field dietary model (Hans-ruled, the substantive one), the three-copies-of-preferences audit, the chip-colour convention, the inline ingredient highlight, and Instacart pack-at-order-time.**) Prior: (**September 2, 2026 — D-WS9-205 minted: the excluded pair-universe boundary, measured at ~257 hidden real relationships and now with a named casualty (the `thin`/`light soy sauce` duplicate no relation records). ⚠️ **ALSO THIS DATE: the whole log was COMPRESSED — 402 entries, 1.04 MB → 784 KB, every ID and all 79 prohibitions verified intact.** Prior: D-WS9-205 (updated **September 1, 2026 (late) — D-WS9-204 minted: Hans's pre-cut-produce ruling. 🔴 **BUY WHOLE AND PREP ON A KNOWN DATE — because Kiwi is a buy-Sunday/cook-Thursday product and pre-cut produce has an UNKNOWN AGE AT PURCHASE. It turns Prep the Week from a chore into a differentiator, and it is independently the highest-waste category under D-WS9-200.** **Exception: shelf-stable jarred forms (minced garlic). The relation table still records DISTINCT — the substitution MECHANISM is open and must NOT be built by overloading a relation label.** Prior: D-WS9-204 (updated **September 1, 2026 (evening) — D-WS9-203 minted: Hans's one-time-backfill ruling. 🔴 **IT INVERTS THE PROGRAM'S METRIC — the review sheet is the safety valve, the auto-accept error rate is the only thing worth gating on, and chat-Claude's residue-RATE gate was wrong in kind (removing junk rows cuts the count 180→79 while raising the rate 33%→36%).** **Review convention is BUG-096's, verified by diffing the files on disk: edit the decision cell in place, rewrite reason as `reviewed YYYY-MM-DD:`, save as -REVIEWED.** Prior: D-WS9-203 (updated **September 1, 2026 (later) — D-WS9-201 and D-WS9-202 minted from the A1 measurement pass; D-WS9-199 VOID-stubbed (reserved, reported unused, D-WS9 keeps its zero-gap record). 🔴 **D-WS9-201 IS A PRECONDITION FOR BLOCK A2, NOT A FOLLOW-UP: the reader takes DIRECT SYNONYM edges only — no transitive closure — because the pilot's largest class over-merged `lard` and `avocado oil` into neutral oil, which is safety-adjacent, not cosmetic.** 🔴 **D-WS9-202 IS THE ONE QUESTION THE WHOLE PIPELINE HANDED A HUMAN — all 8 surviving rows are the generic-vs-colour produce case, and it needs a fourth relation shape (subsumption) or a consistent DISTINCT.** Prior: D-WS9-201 (updated **September 1, 2026 — D-WS9-200 minted: Hans's verbatim food-waste spec from the BUG-190 device pass, and it SPLITS INTO TWO MECHANISMS — pooling (built toward) and remainder-consumption (UNOWNED, needs pack size at composition time). ⚠️ **D-WS9-199 IS DELIBERATELY SKIPPED, NOT LOST: it was handed into the running D-WS9-189 A1 CC prompt as next-available and is therefore reserved. Per the VOID-STUB convention (D-WS9-088), if that block's report comes back without minting it, D-WS9-199 gets a VOID stub at close — a visible heading rather than a silent gap. Nobody mints backward.** Prior: D-WS9-199 (updated **August 31, 2026 — D-WS9-198 minted: Hans's interleaved prep-narration flow preference, captured verbatim at the BUG-186 device pass; rides the next `prep.narrate_steps` content block with BUG-192.** Prior: D-WS9-198 (updated **August 28, 2026, D-WS9-188/189 PROGRAM PHASE 0** — **D-WS9-193 · 194 · 195 · 196 · 197 minted; BUG-177 minted in the bug log.** ❌ **CHAT-CLAUDE'S `conversionRef.subUnit` LEAD IS DEAD — 1 row of 1,570, `parent` is a UNIT STRING not a row reference, and it relates two UNITS ON ONE ROW, never two rows.** ✅ **Option B upheld on better evidence.** 🔴 **THE 750–900 REVIEW FIGURE WAS AN ACCOUNTING ERROR, RELAYED TWICE BY CHAT-CLAUDE: it banded ALL pairs including DISTINCT, and a DISTINCT verdict WRITES NOTHING AND CANNOT REGRESS ANYTHING. Corrected: ~300–600 write-bearing, ~30–60 genuinely needing a human.** 🔴 **AND THE REVIEW MECHANISM WAS NEVER UNDESIGNED — it shipped three times (`--dry-run` → CSV → `--apply`). What is undesigned is whether Hans can COMPLETE one: largest sheet ever finished is 23 rows, and BUG-032's 329-row sheet sits at 0/329, resolved by an Opus judge instead.** 🔴 **D-WS9-197 IS THE ONE THAT BLOCKS: the model labels `lime ~ lime juice` COMPONENT and `lemon ~ lemon juice` DISTINCT, high confidence, IN THE SAME API CALL — so the run would silently fail to author the exact edge this program exists to create.** ✅ **Corrected rubric re-run $0.2986: 8 labels changed, ALL FIXES, ZERO REGRESSIONS.** ⚠️ **Zero of 13 users have `lemon` recurring — D-WS9-188's live case is MILK, the harder display case.** Prior: D-WS9-193 (updated **August 28, 2026, SEQUENCING CORRECTION** — **no IDs minted; pointer unchanged at 193.** ⚠️ **CHAT-CLAUDE TOLD HANS THE COMPLETE D-WS9-188 FIX WAS "WEEKS"; HANS ASKED WHY IT WASN'T A COUPLE OF BLOCKS; IT IS FOUR.** 🔴 **THE ERROR WAS CONFLATING D-WS9-189's CODE HALF WITH ITS DATA HALF — the lemon case needs the `IngredientRelation` SUBSTRATE and D-WS9-182's POOL MATH, not the ~2,000-pair authoring pass, the rubric re-run, or the 750–900-pair review queue.** **Those add coverage and ship no new behaviour; they can run any time after the substrate lands.** ⚠️ **§27's "treat in-chat numbers as beliefs" had only ever been applied to figures chat-Claude RELAYED. It applies to ones it INVENTS — an unmeasured estimate steers scope as hard as a wrong measurement.** Prior: D-WS9-193 (updated **August 28, 2026, D-WS9-188 RULED BY HANS** — **no IDs minted; pointer unchanged at 193.** ✅ **OPTION (b): SHOW BOTH, ANNOTATED — THE APP DISPLAYS, THE USER DECIDES.** Captured verbatim, both examples, because each settles what the other does not. 🔴 **THE RULING DISSOLVED THE BLOCKER RATHER THAN ANSWERING IT: chat-Claude had said (b) and (c) both require comparing quantities across units that do not compare, "the common case, not the edge." THAT IS ONLY TRUE IF THE APP COMPUTES, AND IT DOES NOT — displaying two numbers needs no conversion.** ✅ **THE ARITHMETIC RULE NEEDED BOTH EXAMPLES: same unit → SUM and show the split (lemons, "so I'm getting 5 this week"); un-comparable units → SUM NOTHING, default the order line to the RECURRING quantity with the need beside it ("I can just buy my recurring item").** ⚠️ **SCOPE BOUNDARY: "if we usually have enough left over" is Hans describing HIS OWN mental model, not requesting consumption history, pantry inventory or leftover prediction. Do not build them off a subordinate clause (§8.1).** ⚠️ **AND THE LEMON EXAMPLE SPLITS THE BUILD — zest + juice + whole lemons are ONE purchase (D-WS9-189 relationship 2 / D-WS9-182's pool), so the lemon row's "3 for meals" is the OUTPUT of something unbuilt. The milk half needs nothing and covers 12 of the 14 live groups; the lemon half covers 2 and waits.** Prior: D-WS9-193 (updated **August 28, 2026, BUG-174 PHASE 0 (read-only, FRESH CC CHAT)** — **D-WS9-192 minted; BUG-176 minted in the bug log.** ⚠️ **THE FRESH CHAT REFUTED ITS OWN PROMPT AND TWO CANONICAL DOCS. "29 of 31 duplicate groups differ ONLY in unit" IS WRONG — EXACTLY ONE IS A SPELLING VARIANT.** The figure counted differing unit STRINGS and never checked `canonicalName`; **for 14 of the 31 the NAME differs too, and the merge groups on `canonicalName`.** 🔴 **CONSEQUENCE — D-WS9-188 IS PROMOTED FROM "not urgent, no ruling sought" TO THE LARGEST SINGLE CAUSE OF DUPLICATE GROCERY ROWS (14 of 31) AND A RULING IS NOW SOUGHT.** ⚠️ **And a perfect `IngredientRelation` table does NOT reach those rows either — the recurring synthetic is authored from the user's free-text preference, never resolved against the catalog. A relationship between two catalog rows cannot fix a row that was never a catalog row.** ✅ **D-WS9-189's payoff claim re-scoped accordingly — do not sell the program on the milk rows.** ✅ **D-WS9-192 records why the obvious reuse is WRONG: `prepCombineEngine` already ships the exact semantics BUG-174 wants, but it files `pint` as COUNT while grocery files it as VOLUME — converging them would silently change Cook Mode.** ⚠️ **THIRD AGGREGATE-BY-CAUSE FAILURE IN THIS ARC (§27.5, after BUG-101 and the D-WS9-189 rubric) — and the first one CHAT-CLAUDE propagated into three canonical docs before it was caught.** Prior: D-WS9-192 (updated August 28, 2026, **D-WS9-189 PHASE 0 + 60-ROW CLASSIFICATION PILOT** — **no IDs minted; pointer unchanged at 192.** ⚠️ **THE PILOT REPRODUCED THE EXACT FAILURE HANS NAMED, UNDER A RUBRIC THAT COULD NOT HAVE KNOWN ABOUT IT — 25 right / 3 wrong / 2 abstained / 1 contested of 31 ruled pairs, and ALL THREE WRONG ARE THE PEPPERCORN RULING.** **Rubric defect, not model defect; $0.34 to re-measure.** ✅ **SUBSTRATE RULED: Option B, a join table — the only shape that holds relationship 2 with a yield factor, and the only one whose mistakes are reversible rows rather than deleted ingredients (the BUG-096 pattern).** 🔴 **BUILD IS GATED ON BUG-174 — `bucketKeyOf` never normalises the unit, and `milk`/`Whole milk` already share an `ingredientId` and still render twice, so PERFECT relationship data leaves duplicates behind.** ⚠️ **Same prerequisite blocks BUG-137 — one prerequisite, two programs.** ⚠️ **AND RELATIONSHIP 1 WAS ATTEMPTED BEFORE AND SILENTLY FAILED — 16 inert `IngredientAlias` rows, BUG-175.** ✅ **A deterministic processing-boundary guard traps 424 of 1,997 pairs and would have caught all three errors with no model involved.** **BUG-174 and BUG-175 minted in the bug log.** Prior: D-WS9-192 (updated August 28, 2026, **Plan Options Phase 0 + Hans's plan-flow reframe** — **D-WS9-191 minted: the PLAN-FLOW REDESIGN**, and ⚠️ **D-WS9-190 is CANCELLED BY IT — Hans's own words: "we can leave that screen alone for now because it's going away soon."** ⚠️ **PHASE 0 FOUND BUG-159 IS BLOCKED AT THE DATA LAYER, NOT THE LAYOUT LAYER — `WizardPlanCandidate` carries `mealTitles: string[]`, a flat array of strings, with nowhere a description could live.** That finding is what turned a styling pass into a flow redesign. Prior: D-WS9-191 (updated August 28, 2026, **small-block device pass** — **D-WS9-190 minted: the PLAN OPTIONS screen pass**, from Hans's unprompted design feedback on the screen where the plan is actually chosen. ⚠️ **It carries a USER-VERIFIED product finding: BUG-163's description shipped on the DRAFT REVIEW screen and Hans's verdict was that it does not help him decide there — the plan CHOOSER is where it belongs (BUG-159).** ⚠️ **AND D-WS9-189's SIZING METHOD HAS A PROVEN BLIND SPOT — head-noun clustering MISSES EVERY COMPONENT RELATIONSHIP; see its amended entry.** Prior: D-WS9-190 (updated August 27, 2026, **grocery-correctness device pass** — **D-WS9-189 minted: the INGREDIENT-RELATIONSHIP PROGRAM.** ⚠️ **It is the arc's biggest scope call and it came from Hans naming the pattern rather than the instance:** *"my concern is we're going to keep playing whack-a-mole with these cases."* **It supersedes the per-food approach in BUG-137, generalises D-WS9-182 beyond citrus, and is the reason BUG-170, BUG-161 and BUG-137's sugar/oil instances are deliberately NOT being fixed one at a time.** Prior: D-WS9-189 (updated August 25, 2026, **Root-B Phase 1/2 audit + device pass** — **D-WS9-185 ✅ BUILT**, and ⚠️ **its first real run immediately confirmed BUG-137's blocker: folding garlic yields ZERO bucket merges, leaving 22 lists with two garlic lines.** **D-WS9-187 minted** (⚠️ **a static fixture holds only one half of every pair — authored aliases vs AI-generated rows**); **D-WS9-188 minted** (how a recurring item should RELATE to a meal's need — design question, deliberately separated from BUG-150's defect). Prior: D-WS9-187 (updated August 25, 2026, **Root-B Phase 0 audit** — **D-WS9-186 minted and RULED**: grocery overrides are NOT preserved across a plan change (⚠️ **the list is DOWNSTREAM of the plan**), and ⚠️ **fresh plans become the test substrate, which splits every remaining catalog item into FORWARD FIXES vs OPTIONAL HISTORICAL BACKFILLS**. **D-WS9-182 CORRECTED** (⚠️ **`Ingredient.subUnit` does not exist — it is a key inside `conversionRef Json?`**); **D-WS9-185 ANSWERED** (3-line dry-run fix; ⚠️ **plus a second gap — no abort point between printed plan and transaction**). Prior: D-WS9-186 (updated August 24, 2026, **BUG-134 Phase 1 audit, HEAD `8c234f0`** — **D-WS9-184 minted and RULED**: commit the 32 pre-existing `scripts/output/` review sheets as the backfill audit trail D-WS7-219 requires. **D-WS9-185 minted**: BUG-134's guard ships with its merge-script wiring UNEXERCISED — ⚠️ **its first real run is BUG-137's garlic fold, and the dry-run cannot preview it**; mitigation ruled into BUG-137's prompt. Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps. Prior: D-WS9-184 (updated August 24, 2026, **BUG-134 Phase 0 audit** — **D-WS9-183 minted and RULED same-session**: no database constraint will enforce grocery-row bucket uniqueness; a post-apply assertion inside the merge script stands in. ⚠️ **Hans's Extras ruling makes `(groceryListId, ingredientId, unit)` legitimately NON-UNIQUE in the domain** — a blanket index would P2002 the user-add route, and the correct partial index cannot be expressed before Prisma 7.4. ⚠️ **D-WS9-182 was AMENDED the same day and is now ✅ RULED** — the compatibility matrix was superseded by Hans's fractional component-pool model; no ID was minted for that amendment. Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps. Prior: D-WS9-183 (updated August 24, 2026, **catalog probe close** — **D-WS9-182 minted**: do two derived products of one fruit SHARE the source count or ADD to it? ⚠️ **Hans's ruling required before BUG-133 can be built, and it gates the citrus fold in BUG-137.** The probe's sharpening is load-bearing: **compatibility is per-(product, product) pair, not per-fruit** — zest and juice share a lime, wedges and juice do not. Prior: D-WS9-182 (updated August 24, 2026, **grocery quantity arc close** — **D-WS9-180 and D-WS9-181 minted, both Hans's.** ⚠️ **D-WS9-180 corrects a design assumption chat-Claude had built on: the user should edit the PURCHASE quantity, not the need — and his consequence is the load-bearing part, that a sticky override removes the need to re-scale the pack when the need changes.** D-WS9-181 is the test database and test environment, owned by WS9A, earned by a hand-entered test ingredient living in the live catalog. Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps. Prior: D-WS9-180 (updated August 21, 2026, **post-push, BUG-126 probe session** — **D-WS9-179 minted**: Hans's pack-coverage observation. 389/1,571 rows carry a purchase pack (24.8%); ⚠️ **measure the split scratch-vs-bought before treating that as a defect** — BUG-121's bought path *creates* the rows with genuine retail packs, so coverage should rise as it lands. ⚠️ **And a bought component needs its OWN nutrition ref, not the sum of the scratch components it replaces** — a consequence for BUG-122's convention and BUG-121's design that neither entry recorded; **hand D-WS9-179 to the BUG-121 commissioning prompt alongside D-WS9-178.** Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps. Prior: D-WS9-179 (updated August 21, 2026, **BUG-096 merge block** — **D-WS9-176, D-WS9-177 and D-WS9-178 minted.** **D-WS9-176 is the ruling that made BUG-096 worth building**: the resolver becomes ALIAS-AWARE, because `ingredientResolve` is the only creating path and upserts on exact `lower+trim`, so a merge without it re-accumulates — **re-accumulation was the mechanism that produced the collisions in the first place.** ⚠️ **D-WS9-178 MUST BE HANDED TO BUG-122, NOT RE-INFERRED:** macros describe the ingredient AS PURCHASED; "cooked" is a preparation note on the step, never a different ingredient. **BUG-032's entire wrong-food class came from a matcher inferring a convention instead of being given one.** D-WS9-177 records post-merge cleanup (drop the now-dead `Ingredient.aliases` column; add an `enrichIngredients` DI seam). Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps. Prior: D-WS9-176 (updated August 21, 2026 — **D-WS9-175 minted**: dual-path dishes render SCRATCH by default, bought only on explicit user selection. ⚠️ **Needs a per-component fallback** — 9 components have `scratch=0` and a literal filter drops them silently. ⚠️ **And Phase 0 REFUTED roadmap row 3c's claim that the data model already shipped** — `componentSelections` has NO originating writer; selection persistence is unbuilt, redline queued. Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps. Prior: D-WS9-175 (updated August 21, 2026 — **D-WS9-174 minted**: the beta gate — correctness defects of the *"I just made that"* / *"why do I have to buy that"* class ship BEFORE hosting; features may ship after. ⚠️ **It cuts across roadmap sequencing by design** — first application splits roadmap row 3c, holding the store-bought toggle in WS9B while the dual-path FILTER (BUG-121) moves pre-beta. Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps. Prior: D-WS9-174 (updated August 19, 2026, **correctness block close** — **D-WS9-171 to -173 minted**: grocery edits are list-local; BUG-093's two-stage fix with Hans's worked example as the acceptance criterion; and D-WS9-159 resolved as accepted-unreachable. Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps. Prior: D-WS9-171 (updated August 19, 2026, **correctness-block commissioning** — **D-WS9-170 minted**: the grocery generating-overlay reinstatement (fix-pass item 3, correctly NOT built as expectation copy on a screen that renders after the wait; the deleted overlay idea returns). Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps in the D-WS9 range. Prior: D-WS9-170 (updated August 19, 2026, **Block 2e Part 4 close** — **D-WS9-165 through -169 minted**: the Tell Kiwi three-path restructure, the Plan Review header + tint-primary panel, Home’s contextual and deliberately asymmetric panel roster, the teaching-arc gradient treatment with `Colors.bridge` retired, and ⚠️ **D-WS9-169, Hans’s proposal to assemble plans from a user-selected meal shortlist — recorded as a PROPOSAL, unscoped and unruled, owner a new block between WS9 close and WS9A.** **D-WS9-162 was AMENDED, not superseded**, at the same close: the rule now reads *one dominant terracotta emphasis per screen, tint or fill.* Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps in the D-WS9 range. Prior: D-WS9-165 (updated August 17, 2026, **Block 2e mid-block** — **D-WS9-159 through -164 minted**: composted read-only, SectionLabel convention, draft customizability line, the ⚠️ **one-terracotta-fill-per-screen palette rule** (Hans-ruled by premise-challenge against chat-Claude's proposal), the Tell Kiwi rebuild, and ⚠️ **`app/**` sitting outside the test glob**. Fresh-pulled and heading-grep verified before minting per §29.2; zero gaps in the D-WS9 range. Prior: D-WS9-159 (updated August 13, 2026, **2c close** — **D-WS9-157 and -158 minted**: Block 2e scope, Plan Review + Home make-lane. Prior: D-WS9-157 (unchanged — the B+ ruling landed as an amendment to D-WS9-155, no new ID; updated August 11, 2026, **Block 2c close-out** — **D-WS9-154, -155, -156 minted**; prior same-day: **D-WS9-151, -152, -153** after the 2c Phase 0 audit; prior same-day: **D-WS9-149 and D-WS9-150**: image hosting/curation mechanism (owner WS9A), and the WS7-10-before-WS7-9 roadmap swap. Fresh-pulled and verified by heading-grep before minting per §29.2; zero gaps in the D-WS9 range. ⚠️ **A resume handoff written the same day stated 148 as next-available and was WRONG — 148 already had an entry.** `max(heading-grep, pointer line)` is the authority, never a handoff line.)))))))))))))))))))))
