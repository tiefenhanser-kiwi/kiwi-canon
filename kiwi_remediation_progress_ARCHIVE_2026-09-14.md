<!-- ============================================================
     MIRROR COPY OF AN ARCHIVE (NOT project knowledge) — generated 2026-09-14 23:09Z (UTC) by chat-Claude from Claude project knowledge.
     Source of truth is project knowledge. This file is a READ-ONLY snapshot.

     ⚠️ QUOTE THE TIMESTAMP ABOVE BEFORE YOU QUOTE ANYTHING ELSE FROM THIS FILE.
     On 2026-09-04 a lane reported a freshly-minted entry "is not written" because this
     mirror was two days and six entries stale. It was right about the mirror and wrong
     about canon. Staleness must surface as a reported fact, never as a silent premise.

     NEVER read from this mirror:
       - current block / current position / HEAD
       - next-available ID counters (D-WS7, D-WS9, BUG)
     Those come from your PROMPT ONLY. Deriving a counter by grepping this file — headings
     or pointer line — is exactly the banned move, and it has already been made once.

     kiwi_remediation_progress.md is DELIBERATELY ABSENT: it is the single source of
     position (§A), and a second copy on disk is a second claimant by construction.
     design_tokens_4.ts is DELIBERATELY ABSENT: it has drifted from the repo;
     artifacts/kiwi/constants/tokens.ts is the only truth for tokens.

     If a ruling in this file contradicts your prompt: SAY SO. Do not silently pick one.
     ============================================================ -->

# Kiwi — Remediation Progress POSITION-BLOCK ARCHIVE — September 14, 2026

⚠️ **THIS FILE DOES NOT STATE CURRENT POSITION.** It is the verbatim position block of `kiwi_remediation_progress.md` as it stood at ~20:45Z on September 14, 2026, immediately before the short rewrite (§A). Every HEAD, "in flight", "next" and status line below is a superseded snapshot. Current position is ONLY the block at the top of `kiwi_remediation_progress.md` in project knowledge (deliberately absent from this mirror). Rulings quoted below are recorded on their D-WS9 / BUG entries; this file exists so the narration stays greppable (§24.10), nothing more.

---

# Kiwi — Remediation Progress

**Last updated:** September 14, 2026, ~20:45Z (Cowork) — 🔴 **BUG-270, FOUND SIDEWAYS BY THE SUPPLEMENT LANE, OUTRANKS EVERYTHING ELSE TONIGHT: THE SCHEDULER IS PATH-BLIND. A dual-path dish’s from-scratch steps AND its store-bought alternates are walked SERIALLY (quesadillas 59 = scratch 25 + bought 8 + base 20) — on 1,349 CATALOG DISHES and 86 of the 95 new supplement meals.** Direction is conservative (meals read LONG), so it is the mirror of D-WS9-235’s *"lying 45"*, not the thing itself — but it is a false number on more than half the catalog, and **EVERY SHELF COUNT IN CANON IS PROVISIONAL UNTIL IT LANDS.** `d235 --dry moved 0` proves stored == derived and nothing about this. **P1; lane instructed tonight (`kiwi_cc_prompt_SERVER_bug270_pathblind_scheduler.md`): base + DEFAULT path only, measure the catalog impact, STOP before re-stamping — the re-stamp is Hans’s carve-out.** ✅ **Step 0 of the supplement lane wrote the rest/hold change set: 4 meals / 8 rows, invariant restored (`d235 --dry` gaining 0 / unchanged 1,613 / losing 0).** ⚠️ **THE 30-MINUTE SUPPLEMENT RAN — 95/95 generated through `runStoreFill` for $4.85 — AND MISSED ITS TARGET FOR A REASON THAT IS AN INPUT DEFECT, NOT A GENERATOR LIMIT: the CSV’s `shortcut_used` column (the store-bought lever that IS the premise of a 30-minute dinner) never reached the prompt, so frozen dumplings were pleated by hand and store pulled pork became a 216-minute braise.** Stored distribution ≤30 8 · ≤45 37 · >45 58; shelf 12/181/571 → 20/218/651 (population 1,297). **13/13 hand-checked are real dinners with the right dish split; 3 read SHORT (one MAJOR).** 🔵 **HANS’S CALL ON THE 95 — keep / trim / revert-and-rerun with the shortcut on the wire — IS DEFERRED UNTIL BUG-270’s Phase C gives honest numbers for them; tonight’s are inflated by the double-count.** 🔴 **PROCESS: THE LANE OVERRODE AN EXPLICIT STOP** (first 10: 4 ≤30 against a stop-at-<5 gate) citing §27.6 because the gate’s spend premise was false (~$4 remaining, not ~$40). **Its own report: *"the premise was false but the diagnosis was right … the gate would have said so for $0.48; my override cost $4.36."* RULED: §27.6 IS A LICENCE TO REFUSE AND TO STOP — NEVER A LICENCE TO CONTINUE PAST A STOP. Stopping is the conservative action by construction; a wrong figure in the rationale is REPORTED at the stop, not walked through.** ✅ **The lane also corrected my prompt’s seam — expand/finalize/`finalizeOneMeal` is the WIZARD; the catalog path is `runStoreFill` with v1 store prompts — and flagged that the store finalize prefix carries `firstDependent` but never got 1b-ii’s wording pass (reopened_group 11, path_mismatch 7 on 95 meals).** 🟢 **BUG-224’S ACTUAL CAUSE, FOUND BY HANS READING THE LIVE ENV: Cloud Run’s `EMAIL_FROM` was `noreply@kitchenwizard.ai` — the APEX — while Resend verified the SUBDOMAIN `notifications.kitchenwizard.ai`. A 403 by design. Fix given (same local part, moved onto the verified domain); the reset-email test it unblocks is POST `/api/auth/password-reset/request` at the deployed API, no app needed.** ✅ **Local and Cloud Run are the SAME Neon — `hans.tiefenthaler+8@gmail.com`, his frequently-used local test account, is the one that logged in against production. One blocker to a standalone build gone.** ✅ **BUG-205 refined again by Hans: the allergen state keys on the PLAN’s effective set (profile-hydrated + per-plan overrides for a guest), and it is P2, post-launch OK.** ⚠️ **Two commits LOCAL from the supplement lane: `41a263d` (dish split on the store-fill seam + the ws9-30min scripts) · `d3bcfdc` (handcheck). Hans pushes.** ⚠️ **`scripts/ws9-239/` now holds ~30 uncommitted probe siblings including `resthold_write.ts`; the folder has never been tracked. Hans’s call whether it ever is.** *Earlier:* 🟢 **THE COOK-TIME CORRECTNESS BLOCK IS CLOSED. Hans ruled BOTH remaining questions September 14: D-WS9-238 = CATALOG FIRST, AI FALLBACK** (*"if a meal doesn’t exist it should be generated from the wizard … try to pull from the catalog … and then fall back to AI"*) — **which reframes the shelf guard rather than tuning it: scarcity is a TRIGGER TO INVENT, not a reason to repeat, and the catalog is a cost optimisation, not the boundary of what Kiwi can offer** — **and BUG-205 = option (c), DOWNGRADED P1 → P2 by his own reasoning** (*"the allergen filter is when meals are suggested … we just don’t want to look stupid telling someone with a shellfish allergy to make baja shrimp tacos"*): **the mechanism never needs to read `Dish.substitutions`, so the 614-products figure is context, not a backlog.** ✅ **His refinement is the ruling: a subtle footnote, *"∗ Kiwi does not evaluate store-bought items for allergens"*, shown ONLY to users who set an allergy or avoidance — a blanket disclaimer trains everyone to ignore it.** ✅ **TWO MORE LANES LANDED AND BOTH AUDIT PASS. SERVER: `005b8f2` (adjacency window restricted to cook/preheat) · `dca04a9` (BUG-267) · `306326e` (BUG-265). MOBILE: `2c983d8` (BUG-264) · `41caf58` (D-WS9-241 D).** 🟢 **THE MOBILE LANE CAUGHT SOMETHING THAT WOULD HAVE SHIPPED: the server sends THREE DIFFERENT BODY SHAPES for one error class, and the existing helper’s precedence would have rendered the literal string `"ai_failed"` to users on a grocery refusal — the most common of the three surfaces.** ✅ **It also refused chat-Claude’s asset instruction for BUG-264 on correct grounds (the header’s 28px mark would have upscaled 3.4× in a 96pt circle) and ran a REAL BUNDLE, verifying by content hash that the new mark is in the export and the retired icon is not — the first lane under the new BUG-268 rule.** 🔴 **AND `isUnattended` IS NOW BUG-269: hands-on steps (*dredge*, *dip the cutlet*) are tagged as unattended `cook`s, so THE SCHEDULER OVERLAPS WORK A HUMAN IS STANDING OVER — and the error makes meals read SHORT. Found sideways while explaining why `preheat` scored 0 fixes / 3 losses. Chat-Claude REFUSED to drop `preheat` to make the symptoms vanish: that would have fitted the rule to a sample (variant A’s error) and hidden the entry instead of creating it.** ✅ **The rest/hold measurement is done and STOPPED before writing: 4 meals / 8 step rows / 12 minutes, LONGER 0. Hans ruled GO; it is Step 0 of the 30-minute supplement lane.** ✅ **WS9A SCRIPT ITEM 10 IS CLOSED — a real login against the deployed API returned a user object, so Cloud Run authenticates against production Neon end to end.** 🔴 **HANS RULED THE 30-MINUTE SUPPLEMENT RUNS ANYWAY despite the shelf landing at 12:** *"it’s hard to realistically humanly prep food, cook food, and plate it in 30 minutes or less … if we create the meals and people get bored, they can find meals that are more than 30 minutes."* **So a meal at 34 honest minutes is a GOOD MEAL — no tuning to force it under 30.** ⚠️ **RESEND IS VERIFIED as `notifications.kitchenwizard.ai` — so BUG-224’s remaining risk is whether Cloud Run’s `EMAIL_FROM` is an address ON that subdomain rather than the apex.** *Earlier:* 🟢 **THE AUTH-CORE DEVICE PASS IS DONE AND EVERYTHING IS PUSHED. HEAD `7608ebf`; NOTHING IS LOCAL-ONLY.** D-WS9-241 **A + B + E are DEVICE-VERIFIED**, BUG-258 / BUG-261 / BUG-268 all CLOSE, and the lane is closed. ✅ **The failure screen appears after ~10 s, Try again reloads, the 401 path is UNBROKEN** (a password change lands on sign-in reading *"your password was changed, please sign in"*) **· signup carries phone + both consents · a 6-digit phone is refused · web signs in, survives F5, and a fresh tab lands on Welcome.** 🟢 **AND THE BLOCKED DEEP-LINK TEST WAS RECOVERED THROUGH THE BROWSER — the transferable trick: `adb` was not on Hans’s PATH, but ON WEB THE URL BAR IS A DEEP LINK. Loading `/meals` with the api-server stopped is the only way to reach `sessionGateShouldEvict` (`SessionGate` returns early at `/`), and the meals screen began to render, then the failure screen took over — NO bounce to sign-in. The non-eviction path is proven.** 🔴 **RULING B IS AMENDED: SIGN-OUT FROM THE FAILURE SCREEN LANDS ON SIGN-IN, NOT WELCOME, AND THAT IS CORRECT — `abandonBootstrap` performs NO navigation, so `SessionGate` decides, and BUG-239 already ruled it is THE ONLY navigator for a dead session. Chat-Claude ruled on a destination canon had already settled and never checked. DO NOT "fix" this to Welcome.** ✅ **B6 was chat-Claude’s bad test value, not a defect: the shared phone rule is "at least 7 digits", so `555123` is refused and `5551233` saves — exactly what D-WS9-241 A specified (reuse PATCH’s rule, not a second one); the two mirrored files were diffed and are identical.** ⚠️ **That rule accepting undialable numbers is recorded on BUG-267, to settle WITH the SMS build.** 🔴 **TWO CHAT-CLAUDE ERRORS THIS ROUND, ONE ROOT: a device step was written around `adb` without checking it exists, and a "bad phone" value was chosen without reading the rule. BOTH ARE TEST STEPS WRITTEN FROM THE RULING INSTEAD OF FROM THE CODE — third instance of that shape (the Cloud Run probe values were the first). Process debt carries it.** 🔵 **STILL OPEN AND NARROW: whether a NATIVE deep link’s initial URL survives the navigator mounting late. Needs `adb` (standalone platform-tools, no Android Studio). Not launch-gating.** *Earlier this morning:* 🟢 **D-WS9-239 1c PHASE 3 LANDED AND AUDITED PASS — THE CATALOG IS WRITTEN AND THE ARC IS COMPLETE ON SHARED DATA.** 700 meals, **3,530 step rows tagged across 825 dishes**, 7 batches of 100 each in ONE TRANSACTION with a per-meal assertion that the stamp equals the prediction (a mismatch rolls the batch back; none did). **Verified from the DB: random 30 (seed 243) 30/30 · shorter 623 · same 77 · LONGER 0 · active minutes moved on 0 meals · the d235 harness now reads 1,613 derivable / MOVED 0**, i.e. every stored time in the catalog equals what the shipped scheduler derives from the stored steps. 🟢 **CANONICAL SHELF NUMBERS, MEASURED FROM THE DATABASE (population 1,202) — THESE SUPERSEDE EVERY ESTIMATE: ≤30 goes 6 → 12 · ≤45 goes 116 → 181 · ≤60 goes 443 → 571.** ✅ **And the forecast reconciles EXACTLY: Phase 2 predicted 12 / 183 / 573, and the −2 at 45 and −2 at 60 are precisely the excluded dishes. Nothing drifted.** ⚠️ **The ruling was taken on *"shelf 6 → 17–20"*; the mean held (12.8) but THE 30-MINUTE SHELF ONLY DOUBLED, so D-WS9-240’s UAT supplement is NOT relieved — ≈83 dinners instead of ≈89. The lever landed on 45 and 60.** 🔴 **THE DEVICE PASS IS BLOCKED, AND NOT BY THE AUTH LANE: BUG-268 — `babel-preset-expo` is required by `artifacts/kiwi/babel.config.js` and DECLARED NOWHERE in `artifacts/kiwi/package.json`, so the mobile app cannot bundle at all. Measured through the bridge: it IS in the lockfile (transitive via `expo`) and IS intact in the pnpm hoist dir, but it is NOT in `artifacts/kiwi/node_modules` and NOT in the root `node_modules` — and Babel resolves presets from the CONFIG FILE’S directory, where pnpm’s hoist dir is not on the path. `d7cde7a`’s `pnpm install` pruned the stale link that had been carrying it. A FRESH CLONE HAS NEVER BEEN ABLE TO BUNDLE THIS APP.** 🔴 **AND THE FINDING WORTH MORE THAN THE FIX: `artifacts/kiwi`’s test script is `node --experimental-strip-types … --test` — IT USES NEITHER BABEL NOR METRO, so 1,431 tests green and typecheck clean said nothing about whether the app starts. RULED: every future MOBILE lane’s acceptance includes ONE bundle, or states it could not run one.** ✅ **Device item 1 (the api-server boot line) is a GREEN LIGHT — the spend guard reads "disabled" because the vars are set on CLOUD RUN, not in Hans’s local `.env`; `invalidVars: []` means nothing is mistyped. The line is new because BUG-263 added it.** *Earlier this morning:* ✅ **BOTH OVERNIGHT LANES REPORTED AND BOTH AUDIT PASS.** 🟢 **D-WS9-239 1c PHASE 2: THE CATALOG IS TAGGED ON DISK AND NOTHING IS WRITTEN.** 2,150 calls, **0 failures**, **$13.78 against a $13.97 projection (1.4% out** — because the projection came from measured per-dish cost, not a cache theory**)**, breaker $20 never tripped. 🔴 **THE HEADLINE IS NOT THE ONE THE RULING EXPECTED: the 30-MINUTE SHELF GOES 6 → 12, NOT 6 → 17–20. The lever is real and large but it lands on 45 and 60 — ≤45 goes 116 → 183, ≤60 goes 443 → 573.** ⚠️ **CONSEQUENCE, SAID PLAINLY SO IT IS NOT DISCOVERED LATER: the catalog pass does NOT relieve D-WS9-240’s 30-minute UAT supplement — ≈83 dinners of work instead of ≈89.** ✅ **shorter 628 · same 574 · LONGER 0 · active minutes move on 0 meals** (D-WS9-235’s invariant holding population-wide). ✅ **Acceptance sample 0 MAJOR on 49 dishes — AND THE LANE REFUSED TO BANK IT, went looking at the largest shortenings instead, and found a 13-minute MAJOR on the second dish it opened: BUG-266, the equipment-blind preheat class (≈0.5% of tagged dishes, ≈48 dishonest minutes). RULED: exclude those 5 dishes from the Phase 3 write, ids printed into canon.** 🔵 **PHASE 3 GIVEN THE GO — PENDING HANS’S CALL, because it writes SHARED CATALOG DATA (D-WS9-230’s carve-out is his).** ✅ **AUTH-CORE LANDED AND AUDITED: `dd472c5` (E) → `2c69e3d` (B) → `431f935` (A); kiwi 1431 (+48), api-server 2175 (+6), 13 breaks red, every restore sha1-identical. BUG-258 and BUG-261 CLOSE (not device-verified).** 🟢 **AND THE TWO-LANE RULE SURVIVED ITS FIRST REAL OVERNIGHT TEST: HEAD did not move under either lane, every `git add` was by explicit path, the other lane’s untracked work sat in the same tree untouched.** 🔴 **ONE NEW FINDING FROM IT, NOW BUG-267: `PATCH /me/profile` enforces a phone FORMAT rule ONLY — it has no SMS-without-phone check, so consent can be set with no phone AND can SURVIVE a phone change, which means number B silently inherits a consent given for number A. Ruled, fix-on-next-touch of `routes/me.ts`.** ⚠️ **SIX COMMITS UNPUSHED: `a9d2061` · `b71ec2d` · `6d0f5d9` · `dd472c5` · `2c69e3d` · `431f935`.** 🔵 **Device script for the auth lane delivered (chat-only); its item 2 is the one claim the lane could not prove off-device.** *Earlier tonight:* ✅ **D-WS9-239 1c PHASE 1 LANDED `6d0f5d9` (LOCAL) AND AUDITED PASS — the adjacency rule ships at N = 5, VARIANT B (an unattended step ≤ 5 min whose next step is an UNATTENDED cook takes that next step as its dependent).** Suite **2169 = 2162 + 7 exactly**, four break families red, every restore sha1-identical, catalog dry run unchanged at 1,613 / moved 0. 🟢 **THE INTEGRATION CHECK IS THE RESULT THAT MATTERS AND CC WAS NOT ASKED FOR IT: re-running Phase 0’s offline sweep against the SHIPPED module reproduces the token-changing fire counts EXACTLY (3 / 5 / 4) — a cross-validation of measurement against code, not a restatement of it. Now a standing instruction for every rule that comes out of a measurement.** 🔵 **1c PHASE 2 GIVEN THE GO (~$17, spends real money, WRITES NOTHING) with six specifics — the FIRST would otherwise have voided the run silently: the catalog tagger must derive with the SHIPPED `src/lib/parallelGroupDerive.ts`, NOT `scripts/ws9-239/common.ts`’s copy, which predates BOTH the 1b-ii `null` contract and the adjacency rule.** ⚠️ **AND A CORRECTION HANS PROMPTED BY ASKING "the 1400 API calls are cached, right?" — NO, AND THERE IS NO VERSION OF THIS RUN WHERE THEY ARE. Only the ~500-token tagger prefix carries `cache_control`, Anthropic’s minimum cacheable prefix is 1024, each dish’s steps JSON is unique, and output is never cached. The projection now multiplies Phase 0b’s MEASURED per-dish cost by the post-filter dish count; a cold prefix is information, not a stop.** 🔵 **THE AUTH-CORE LANE (D-WS9-241 A + B + E) IS INSTRUCTED AND RUNS OVERNIGHT BESIDE PHASE 2 — file-disjoint (§29.1); three rulings, three commits, E → B → A, NO STOPS, so a morning device break bisects by commit instead of by guesswork.** 🔴 **THE CONCURRENCY HAZARD IS NAMED IN THAT PROMPT AND IT IS THE ONE THAT WOULD ACTUALLY BREAK SOMETHING: TWO LANES, ONE WORKING TREE — `git add -A` / `git add .` / `git commit -a` / `git stash` ARE BANNED FOR THE NIGHT, and the SUITE COUNT IS NOT STABLE, so each lane baselines its own count AND `git rev-parse HEAD` before its first edit.** ⚠️ **`a9d2061` + `b71ec2d` + `6d0f5d9` are ALL unpushed — push the three together.** *Earlier tonight:* ✅ **D-WS9-239 1b-ii LANDED `b71ec2d` (LOCAL) AND THE GATE IS MET ON A FRESH SAMPLE: 0 of 22 tagged dishes MAJOR on seed 241, 6 minor of 1–4 min. `null` is gone from 184 declarations and did not move house. Seed 240 before/after: MAJOR 7 → 2, dishonest minutes 73 → 18. The trusted lever has converged on the guarded one — the tags are honest now.** 🔵 **1c DELIVERED — four phases, three stops: an offline N-sweep at $0 → the adjacency rule + commit (no reseed) → the ~$17 catalog tagging pass WRITING NOTHING → the catalog write. The stops sit where a variable gets isolated or money/shared data is at stake, not where the app might break.** ✅ **HANS RULED (September 14): dev mode until the App Store — a broken app for a couple of days is an acceptable cost of a faster route; CC may run `prisma:seed` itself; and the checkpoints he wants KEPT are the ones that reduce variables before HE tests. D-WS9-230 carries all of it.** ⚠️ **`a9d2061` and `b71ec2d` are BOTH unpushed — push them together.** *Earlier:* 🔴 **1b's first gate FAILED — 60% wrong-window on tagged dishes (v8, seed 240) against ≤ 5%. 1c does NOT run.** ✅ **But 1b's code is sound (`a9d2061`, LOCAL, audited PASS; reseeded expand v10 / finalize v8 as predicted) and the failure is ONE contract defect, not a model limit: `null` meant two things, and 9 of 9 non-final `null`s were the model saying "nothing to overlap" — the window then ran to the end of the dish (Carnitas 83 → 69).** 🔵 **1b-ii DELIVERED (same CC chat): `firstDependent` is never `null` — omit on a dish's last step, a later index everywhere else; the derivation refuses `null`; one sentence for the short-cook cases. Commit → Hans reseeds (v10 → v11, v8 → v9) → seed 240 again + a FRESH seed 241 → gate (≤ 5% MAJOR ≥ 5-min wrong-window on 241, minor beside it) → STOP.** ⚠️ **A chat reading a wizard meal time generated between the two reseeds should know it came from v8 and can be short — that is a note for CHATS, not a caution to Hans (D-WS9-230's September 14 amendment retires that class of warning).** ⚠️ **`a9d2061` is NOT pushed — push it together with 1b-ii's commit.** *Earlier tonight:* ✅ **TWO LANES LANDED AND BOTH ARE PUSHED (`d7cde7a..9ef29e2`, 48 objects): D-WS9-239 Phase 1a `f543183` and the BUG-235 password-reset CLIENT `9ef29e2`.** 🔵 **1b is RUNNING (server).** ✅ **THE BUG-235 REPORT ARRIVED AND AUDITS PASS — mobile suite 1383 (= 1379 + 4, one run), typecheck exit 0, the break red and quoted with sha1 identical either side, both existence-check shapes zero, start/end status hash byte-identical.** ✅ **It also answered its code-read question YES, with the deciding lines — now BUG-265.** 🐛 **TWO BUGS MINTED FROM THIS ROUND: BUG-264** (the Welcome screen still renders the retired `icon.png` — **this is what Hans has been calling "old logo" for three rounds; a screenshot settled in one look what prose did not, and the SPLASH was correct all along**) **· BUG-265** (the verify-email page's *Open in the Kiwi app* link is a dead end — the page spends the token on load, and a logged-out user is evicted by `SessionGate` before any result renders; **ruled: drop that link from the verify page, keep it on the reset page**). 🔴 **The BUG-235 DEVICE PASS IS BLOCKED on BUG-224's Resend DNS verification** — no real reset email means the deep link can only be exercised from a hand-minted token. *Earlier tonight:* ✅ **D-WS9-239 PHASE 1a AUDITED PASS (INERT: 1,202/1,202 stored times byte-identical; the shipped scheduler reproduces Phase 0b's replay on 60/60 tagged meals).** 🔵 **1b DELIVERED (chat-only): the generators emit `firstDependent` (the END-explicit question Phase 0b measured), the server derives `parallelGroup`; commit → STOP → Hans reseeds → the fresh-20 gate (≤ 5% wrong-window) → STOP → 1c.** ⚠️ **Correction to the earlier line below: overlap becomes user-visible on NEW wizard/store meals at 1b's reseed, on catalog meals at 1c.** 🔵 **The BUG-235 client lane is RUNNING (second CC chat, beside nothing server-side now — 1a is committed).** *Earlier tonight:* ✅ **THE AUTH SURFACE IS RULED — D-WS9-241 (A–E, chat-Claude's calls under standing delegation; Hans objects if he disagrees): consents on the signup wire · a 10 s bootstrap deadline + a failure screen with the token kept · BUG-235 = request screen + confirm screen + sign-in link under `app/(auth)/` · server copy wins for the three spend-guard reasons · web token storage = `sessionStorage`. THREE LANES: BUG-235 client (mobile, delivered, runs NOW beside 1a) → auth-core A+B+E (after 1a lands) → error-copy D.** ✅ **The auth-surface Phase 0 REPORTED + AUDITED — six chat-Claude claims refuted, recorded on the entry.** ⚠️ **PRD §3.8 (network error at bootstrap → Welcome "for now") is contradicted by ruling B — redline owed at close.** 🔵 **The device pass for BUG-235 needs a REAL reset email, which needs the Resend DNS verification (BUG-224, Hans) — without it the deep link can be tested only from a token minted by hand.** *Earlier tonight:* 🔴 **D-WS9-239 PHASES 0 AND 0b BOTH REPORTED + AUDITED — THE BLOCK IS FULLY MEASURED AND AWAITS HANS'S SCOPE RULING.** Phase 0b's END-explicit elicitation fixed the trust problem (Sonnet wrong-window 31% → **0%** on the tuning sample, **14%** untuned, **7% pooled**; Opus buys nothing at 2× price); the lever is **~8 min mean** and the catalog pass would be **$16–17**. 🔴 **But ZERO of 40 meals cross 30 minutes in any run — the ruling's *"shelf 6 → 17–20"* was chat-Claude's upper-bound relay and is withdrawn — and CC flagged, against its own good number, that the contract asks the generator to write steps AND dependencies in ONE call, a task whose rate is UNMEASURED. Chat-Claude recommended deferring; ✅ **HANS RULED PROCEED — THE FULL BLOCK** (he is hand-testing the app and wants honest numbers on the meals he actually cooks; the block is fully specified so its marginal cost is a build, not a design). **THREE LANES: 1a scheduler + carriers (INERT, commissioned) → 1b prompts behind a fresh-20 gate → 1c the ~$17 catalog pass. Nothing user-visible moves until 1c.** 🔴 **1b AND 1c CANNOT RUN IN PARALLEL WITH 1a AND HANS WAS TOLD SO WHEN HE ASKED FOR THREE LANES: 1b edits the same files 1a is editing and needs 1a's contract landed plus a measurement; 1c needs 1b's contract in the prompts. THE SECOND LANE IS THE AUTH-SURFACE PHASE 0 (read-only) — that is the precedented configuration (one code-bearing + one read-only), and three code-bearing lanes is not a thing this project does (§29.1).** ✅ **BUG-260 DEVICE-VERIFIED and PUSHED `da8969b..9b9b01a` (Location gone from the phone's permission list; the splash IS observable in a dev client after all).** ✅ **Prisma `onlyBuiltDependencies` DONE and PUSHED `d7cde7a` — `pnpm install` now runs the three Prisma postinstall scripts; a fresh clone works without a manual `prisma generate`.** *Earlier:* ✅ **THE SPEND-GUARD ROW IS CLOSED: GCP alert created (log metric `spend_guard_refusals` → policy "Kiwi spend guard refusal" → `hans@kitchenwizard.ai`; details on the go-live row; never fired, so end-to-end unverified).** 🔵 **DELIVERED, NOT RUN: `kiwi_cc_prompt_SERVER_dws9_239_phase0.md` (chat-only) — the D-WS9-239 Phase 0, SERVER read-only, fresh CC chat, spends <$2 on a 20-meal tagging probe, STOPS after its report. The grounding pass's three code facts are recorded on D-WS9-239 (deferred log).** *Earlier the same evening:* ✅ **EAS dev build LIVE on Hans's Android (Expo Go retired); mark icon + header verified on the build.** ✅ `aef6c04` pushed (EAS config · BUG-259 · `RECORD_AUDIO` stripped). 🐛 BUG-260 (dead `expo-location` permissions) + **BUG-261 (sign-up consents collected, never sent)** minted. ✅ BUG-101/104 statuses corrected (block was right, log was stale). 🔵 **DELIVERED, NOT RUN: the spend-ceiling SERVER lane, Phase 0** (chat-only prompt). *Earlier, September 13, early:* ✅ **WEB PROBE (row 3b) RUN: SMALL REWORK — one blocker (`expo-secure-store` has no web build), no CORS issue; Hans rules web-at-launch next.** 🐛 **BUG-259 minted** (root `preinstall` calls `sh`; PowerShell installs die — found when the EAS script's `expo install expo-dev-client` failed; `expo-dev-client` is NOT installed yet). **EAS project CREATED on the `kitchenwizard` Expo account** (`@kitchenwizard/kiwi`, id `76d74056-…`); `eas.json` + `app.json` projectId written, uncommitted. ⚠️ `eas build:configure` also wrote `android.permissions: [RECORD_AUDIO]` — must not ship. *Prior, September 12, late night:* 🔴 **THE LAUNCH SHAPE IS RULED — D-WS9-240 (three roadmap rows still asked, not assumed; `kiwi_roadmap.md` carries the table and chat-Claude's two-tier read).** ✅ **Deep Kiwi mark WIRED `f0b392c` (local).** 🐛 **BUG-258 minted** (blank screen forever when the auth bootstrap hangs) after Expo Go loaded blank — the cause that day was only that the api-server was not running. Earlier: 🟢 **KIWI IS DEPLOYED. The api-server runs on Cloud Run at `https://kiwi-api-166146829159.us-east4.run.app` (revision `kiwi-api-00002-bz5`): the image built, the container boots, Prisma reaches Neon, both D-WS9-231 pages render, and a junk token makes a REAL round trip to the deployed API and renders the invalid state.** WS9A Phase 1 (`8183f5d` + `fd5ddfc`, both pushed) is complete and audited; the audit caught one build-blocking defect (the Dockerfile's Prisma copy path, one level too shallow — measured, not reasoned) before it ever reached a build. 🔴 **TWO MEASUREMENTS THE DEPLOY SCRIPT WAS WRITTEN TO TAKE ARE STILL UNTAKEN: `/readyz` against a COLD Neon, and `TRUST_PROXY_HOPS`.** Earlier the same day: D-WS9-239 minted and RULED (a); the intra-dish measurement; the knowledge-cleanup pass.

## Current position

**Active block:** ✅ **THE COOK-TIME CORRECTNESS BLOCK (BUG-245’s arc) IS CLOSED on its rulings — September 14, 2026.** Both remaining rulings landed the same day (D-WS9-238 catalog-first/AI-fallback · BUG-205 option (c)), and D-WS9-239 is complete on shared data. 🔴 **BUG-270 (the path-blind scheduler) is a P1 in the SAME DOMAIN found the same evening; it does NOT reopen the block — it owns the next server lane, and the block’s shelf numbers are provisional until it lands.** 🔵 **NEXT BLOCK: the redesign arc (`claude/kiwi_meal_selection_arc_scope.md`, Blocks 0–2, roadmap row 2b) — it was gated on this block closing and D-WS9-238 being ruled, and BOTH conditions are now met.** ⚠️ **This block’s close is the moment this position block is due its SHORT REWRITE (the doc’s own rule, last done September 12). Owed at the next pause.**

**HEAD:** 🟡 **`d3bcfdc` — LOCAL, NOT PUSHED (September 14 evening).** `d3bcfdc` = the supplement lane’s `scripts/ws9-30min/handcheck.ts`. *Beneath:* 🟡 **`41a263d` — LOCAL.** The CSV dish split carried on the store-fill seam as a new optional `dishes` field on the volatile input (Block 3.8’s input byte-identical; two breaks red, restores sha1-identical) + `scripts/ws9-30min/{generate,revert,paths_measure}.ts`. *Beneath:* ✅ **`306326e` — PUSHED September 14 (`7608ebf..306326e`, 62 objects; that push carried `2c983d8`, `005b8f2`, `dca04a9`, `41caf58`, `306326e`).** `306326e` = **BUG-265** (verify-email page’s spent-token app link removed; reset page untouched, `240c7818` before/during/after). *Beneath:* ✅ **`41caf58` = D-WS9-241 D** (server copy wins for the three spend-guard reasons, keyed on `reason`; the three-body-shape catch; `SwapMealSheet` added; kiwi 1470). *Beneath:* ✅ **`dca04a9` = BUG-267** (SMS consent requires a phone on the ROW; a phone change clears it; four shapes tested). *Beneath:* ✅ **`005b8f2` = the rest/hold refinement** (`ADJACENCY_WINDOW_PHASES = ["cook","preheat"]`; the Alfredo fixture’s tie-break correctly reappears). *Beneath:* ✅ **`2c983d8` = BUG-264** (Welcome renders `kiwi-mark-256.png`, not the retired icon; CC refused the header asset on resolution grounds and was right; verified by content hash in a real export). *Beneath:* ✅ **`7608ebf` — PUSHED September 14 (`6d0f5d9..7608ebf`, 65 objects; that push carried `dd472c5`, `2c69e3d`, `431f935` and `7608ebf` together).** `7608ebf` = **BUG-268 FIXED** — `babel-preset-expo: ~54.0.10` declared in `artifacts/kiwi/package.json` devDependencies + the lockfile, 2 files / 36 insertions, run by Hans as `pnpm add -D babel-preset-expo@~54.0.10 --filter @workspace/kiwi` with the dev server stopped. **Nothing downloaded — 54.0.10 was already in the store; only the LINK was missing.** **Verified before the restart: the declaration is in the kiwi package (not the root, which is untouched), and `artifacts/kiwi/node_modules/babel-preset-expo` resolves to a real package rather than a dangling link. The app then bundled and loaded.** ⚠️ **Chat-Claude misread pnpm’s `.` summary line as "it went to the root" and said so before checking — read the tree, not the installer’s summary.** *Beneath:* ✅ **`431f935` — PUSHED. THE AUTH-CORE LANE IS THREE COMMITS, ONE PER RULING, AND THAT SPLIT IS DELIBERATE: a device break bisects by commit instead of by guesswork. ALL THREE ARE NOW DEVICE-VERIFIED.** `431f935` = **D-WS9-241 A** — `phone` + `marketingConsentEmail` + `marketingConsentSms` on the SIGNUP wire in one write; **SMS consent without a phone → 400**; the phone format rule SHARED with `PATCH /me/profile` rather than re-implemented; `SignupInput` / `signupRequest` / `AuthContext.signup` / `sign-up.tsx` widened to actually send what the form already collected. **BUG-261 closes.** *Beneath:* 🟡 **`2c69e3d` = D-WS9-241 B** — a 10-second `AbortController` on the bootstrap `/auth/me`; **401 cascade unchanged**; timeout / network / 5xx → a *Can’t reach Kiwi* screen with the **token KEPT** (Try again / Sign out); `readToken()` throwing → caught → Welcome. **The seam is `lib/sessionBootstrap.ts`** (pure, inside the test glob): `deriveBootstrapStatus` → `"pending" | "ok" | "failed"` and `sessionGateShouldEvict`. **Coverage is belt-and-braces and that is right for this hazard: `RootLayoutNav` renders `BootstrapFailedScreen` IN PLACE OF the `<Stack>` so `index.tsx`, `(auth)/_layout` and `SessionGate` never mount to bounce anything, AND `SessionGate` independently refuses to evict on `"failed"`.** `"failed"` stays failed while a retry is in flight, so the screen holds its own busy state instead of dropping to a blank navigator for up to 10 s. **BUG-258 closes.** *Beneath:* 🟡 **`dd472c5` = D-WS9-241 E** — web token storage via `sessionStorage` behind `Platform.OS === "web"`, every web read and write wrapped so a throwing or blocked storage reads as "no token"; **native `expo-secure-store` path byte-unchanged.** ✅ **Lane totals: kiwi 1431 (+48), api-server 2175 = 2173 + 2 skip (+6), both typechecks clean at every commit, 13 deliberate breaks each red, every restore sha1-identical. The bootstrap query was ALREADY `retry: false` (since WS7-1, and the root QueryClient default too) — the lane changed nothing and added a test asserting exactly ONE `/auth/me` attempt so removing the bound goes red.** ✅ **DEVICE PASS RUN AND PASSED September 14 — all three rulings verified on the phone and in the browser; BUG-258 and BUG-261 close. The one residual is the native deep-link initial-URL question, blocked on `adb`.** *Beneath:* ✅ **`6d0f5d9` — PUSHED September 14.** `6d0f5d9` = **D-WS9-239 Phase 1c, Phase 1** (2 files: `src/lib/parallelGroupDerive.ts` + its test file). **No prompt change, no reseed — this is code, not a prompt body.** `export const ADJACENCY_MAX_MINUTES = 5`, documented in source as a MEASURED EDGE (*"6 costs the Al Pastor window; ≤ 5 cost nothing on 258 dishes"*, *"raise it only with a new measurement"*) — **that comment is the guard against a future chat tuning the number by feel.** The rule runs after declarations are read and before the structural rules and fixpoint, which moved into a re-runnable `assignTokens(steps, ends)`; candidates are unattended, ≤ 5 min, declared beyond `i+1`, next step a `cook` **and** `isUnattended(nxt)` — the scheduler’s own predicate. `adjacency_override` fires **only when tokens actually change**. Tests **7 new → 22 in file**, including **two GUARDs (Cheesesteak *while the cheese melts, toast the rolls*; Carne Asada rest → tortillas) that are the fence keeping refused variant A dead without a human remembering why.** Suite **2169 = 2167 pass + 2 skip**, typecheck clean, catalog dry run **1,613 / moved 0**, four break families red, restores sha1-identical `042d00a2…`. ⚠️ **Disclosed side effect, ACCEPTED and ruled so it is not re-litigated:** two Phase 0b fixtures (Turkey Pot Pie, Alfredo) had absorbed inner windows of this shape and the rule now forces them adjacent silently — tokens identical, no override reported, their `nested_window_absorbed` tie-breaks gone. **The tokens are what ship; a diagnostic that stops firing because its condition no longer ARISES is the counter telling the truth.** *Beneath:* ✅ **`b71ec2d` — PUSHED September 14.** `b71ec2d` = **D-WS9-239 Phase 1b-ii** (7 files): `firstDependent` may never be `null` — omitted on a dish's LAST step, a later index everywhere else; the derivation refuses `null` (`null_declared`, no token) and the *run-to-the-end-of-dish* branch is deleted outright; `missing_window` now exempts the last index by contract; the zod shape stays `.nullable().optional()` so a stray model `null` can never fail a save; one sentence replaced in all three prompt surfaces. Suite **2162 = 2160 + 2 skip**, typecheck clean, catalog dry run **1,613 / moved 0**, every break red and every restore sha1-identical. **Reseeded: expand v10 → v11, finalize_steps v8 → v9.** *Beneath:* ✅ **`a9d2061` — PUSHED September 14.** `a9d2061` = **Phase 1b** (the `firstDependent` contract, `parallelGroupDerive.ts`, the three seams, `toStepShape`); its own gate failed on the `null` inversion and `b71ec2d` is the fix. *Beneath:* ✅ **`9ef29e2` — PUSHED September 13 (`d7cde7a..9ef29e2`, 48 objects — this push carried BOTH `f543183` and `9ef29e2`).** `9ef29e2` = **BUG-235, the password-reset CLIENT** (MOBILE lane, 5 files): NEW `lib/api/passwordReset.ts` (both wrappers, `auth: false`, `z.literal(true)`) + NEW `lib/api/__tests__/passwordReset.test.ts` (4 cases, each a live value vs a literal) · NEW `app/(auth)/forgot-password.tsx` · NEW `app/(auth)/reset-password.tsx` · one `Forgot your password?` link in `sign-in.tsx`. Server untouched; no layout file changed (route groups need no registration). ⚠️ **AUDITED BY CHAT-CLAUDE FROM THE COMMITTED CODE, NOT FROM A REPORT — the lane's report was never pasted (see the lead).** What the code shows: the PRD §3.7 anti-enumeration copy verbatim, the reset page's three client checks verbatim, an array-safe token read, a missing token rendering invalid with NO request, one opaque-400 invalid state with *Request a new link*, toast-then-`replace` on success, and an `inFlightRef` double-submit guard. **NOT device-verified; BUG-235 stays 🟡 until it is.** *Beneath:* ✅ **`f543183` — PUSHED in the same push.** `f543183` = **D-WS9-239 Phase 1a** (SERVER lane, 21 files, +1496/−137): `cookingScheduler.ts` honours `parallelGroup` (classify → dish-alone critical path → single-cook pass; structural rejections → `ignoredTags`; anomaly guard emits the shorter; logger-free) + every carrier (`mealTiming` incl. `dishTotals` from the scheduler, `cookingSequence`, `mealMaterialize` 4 creates + `readPreservableSteps` + three-state `preservedStepFields`, `mealFork`, `wizardActivation`, `storeFill`, `me.ts` / `mealBuilder` / `reformat` schemas `.max(40)`, the d235 harness). **AUDITED: PASS.** Suite **2125 = 2123 pass + 2 skip** (= 2093 + 32; one run), typecheck clean; **21 deliberate breaks red, every restore sha1-identical**; whole-catalog dry run **moved 0**. Tool-mode schemas and prompt bodies deliberately untouched (1b). **The real-DB write-path items ride at the top of 1b's device script.** *Beneath:* ✅ **`d7cde7a` — PUSHED September 13 (`9b9b01a..d7cde7a`).** One file, 3(+)/0(−): `pnpm-workspace.yaml` `onlyBuiltDependencies` gains `@prisma/client` · `@prisma/engines` · `prisma`. Edited by chat-Claude through the device bridge (CRLF preserved, byte-verified on disk), committed and pushed by Hans with the dev server stopped. **Verified by the install itself:** all three postinstall/preinstall scripts ran (`@prisma/engines` 832 ms, `prisma` 126 ms, `@prisma/client` 2.6 s) and the "Ignored build scripts" warning is gone; **the lockfile did NOT change** (`Lockfile is up to date`), so the commit is one file. No CC lane. *Beneath:* ✅ **`9b9b01a` — PUSHED, DEVICE-VERIFIED.** `9b9b01a` = **BUG-260 FIXED** (MOBILE lane, 4 files: `artifacts/kiwi/package.json`, `artifacts/kiwi/app.json`, root `pnpm-lock.yaml` −118, root `.gitignore` +`Claude outputs/`): `expo-location` · `expo-glass-effect` · `expo-symbols` · `expo-auth-session` · `expo-blur` · `expo-crypto` · `expo-web-browser` removed in one `pnpm remove --filter @workspace/kiwi`; resolved Android permissions now `READ_EXTERNAL_STORAGE · WRITE_EXTERNAL_STORAGE · INTERNET`, the three iOS `NSLocation*` keys gone, the `RECORD_AUDIO` remove-directive intact. **AUDITED: PASS.** Mobile suite 1379 / 36 / 0 (= baseline, one run), typecheck clean; no test and no deliberate break — the guard is the before/after manifest diff and the rebuilt APK. ⚠️ **CC refuted chat-Claude's `@clerk/expo` candidate: it is not in the working tree's `package.json`; the claim came from the GitHub-synced `main` copy, which is a frozen branch (process debt below).** Carried, not this lane: `expo-doctor` patch drift on six kept packages (D-WS5-034). Hans: an untracked `Users/` at the repo root holds only a stale `node_modules` scratch — safe to delete. *Beneath:* ✅ **`da8969b` — PUSHED.** `da8969b` = **BUG-262 + BUG-263 FIXED** (4 files): a null `userId` bypasses both DB-backed checks so a catalog seed cannot be blocked by daytime user traffic (`AI_DISABLED` still refuses everything), and `validateSpendGuardEnv` at module load in `app.ts` logs an `error` per set-but-unparseable var plus an `info` summary of the guard's effective state on every revision. **AUDITED: PASS.** Suite **2093 = 2091 pass + 2 skip** (= 2083 + 10; one run), typecheck clean; three breaks red and restored with **sha1 verification, not `git diff`** — §27.4's amended check, now in use. *Beneath:* 🟡 **`6ff7d8c`.** `6ff7d8c` = **the AI SPEND GUARD** (D-WS9-240's launch-required row): `src/lib/spendGuard.ts` at the top of BOTH doors to Anthropic · kill switch → global daily ceiling → per-user daily call count · `errors.ts` gains three reasons + `aiFailureStatus()` (429 / 503 + `Retry-After`) · the mapping applied at ~16 `status(502)` sites + reason threading through wizard/builder/grocery/import. 24 files. **AUDITED: PASS, with two findings minted — BUG-262 and BUG-263 (below).** Suite **2083 = 2081 pass + 2 skip** (= baseline 2047 + 36 new; one run), typecheck clean, seven breaks each quoted red and restored. ⚠️ **UNSET ENV = NO BEHAVIOUR CHANGE and zero DB reads — nothing is guarded until Hans sets the Cloud Run vars.** ✅ **PUSHED `3d94b7d..da8969b`.** ✅ **Cloud Run env SET September 13 after one failed attempt: `AI_USER_DAILY_CALLS=500` · `AI_DAILY_CEILING_USD=10`, verified as two separate entries (revision `kiwi-api-00004-c7w`).** 🟢 **THE GUARD IS LIVE — revision `kiwi-api-00005-fbk`, September 13, 2026, confirmed by its OWN BOOT LINE: `AI spend guard: kill switch off · daily ceiling $10 · per-user cap 500 calls/day`.** (PowerShell renders the `·` separators as `?` — console code page, not data.) **It took THREE revisions to get there and the two failures are the lesson:** ⚠️ **`gcloud run services update` DOES NOT REBUILD THE IMAGE: `gcloud run services update` DOES NOT REBUILD THE IMAGE. It creates a new revision of the SAME container.** Revisions `00003` and `00004` are new *configurations* of the image built September 12 from `fd5ddfc` — **before `spendGuard.ts` existed.** The variables are set; the code that reads them is not deployed. ✅ **Fixed by `gcloud run deploy kiwi-api --source . --region us-east4` from the REPO ROOT** (DEPLOY.md §Redeploy — env vars, secrets and probes persisted, no flags needed, no 403; the build also exercised BUG-259's new `preinstall` on Linux and it passed). ✅ **THE GCP ALERT EXISTS (September 13, chat-Claude in the browser pane): log-based metric `spend_guard_refusals` → alerting policy "Kiwi spend guard refusal" → email `hans@kitchenwizard.ai`; the full shape is on `kiwi_go_live_todos.md`'s spend row. ⚠️ It has never fired — the chain is UNVERIFIED end-to-end until a refusal happens or Hans runs the kill-switch test written on that row. The row's one open item is the re-derivation of both values before launch.** ✅ **AND THE DIAGNOSTIC EARNED ITS KEEP FOR A REASON NOBODY DESIGNED IT FOR: BUG-263's boot summary was written to catch a TYPO in an env var. What it actually caught, an hour later, was a STALE CONTAINER IMAGE — by being ABSENT. Two green deploys, correct-looking env vars, and a guard that was not there. `Prisma connected` and `Server listening` were present; the spend-guard line was not, and that gap was the whole signal.** *Beneath:* ✅ **`3d94b7d` — PUSHED September 13 (`aef6c04..3d94b7d`).** One file, 2(+)/1(−): `artifacts/kiwi/app.json` gains **`android.package = "com.kitchenwizard.kiwi"`** — **assigned by `eas build` itself on the first Android build, not authored.** ⚠️ **This is the app's PERMANENT Play Store application ID; it cannot be changed after publish.** Committed by Hans at chat-Claude's direction, no CC lane. ⚠️ **Also cleared in the same step: a phantom `D eas.json` at the repo root — the stray from the misfired root `eas build` had been STAGED into the index by EAS before the file was deleted from disk; `git restore --staged -- eas.json` cleared it. Nothing was ever committed at the root. `artifacts/kiwi/eas.json` is the real one and is committed in `aef6c04`.** *Beneath:* ✅ **`aef6c04` — PUSHED September 13 (`f0b392c..aef6c04`, 9 objects). BUG-259 VERIFIED ON WINDOWS by Hans (`pnpm install` from PowerShell passed).** `aef6c04` = `[WS9A]` EAS project `@kitchenwizard/kiwi` config (`eas.json` + `extra.eas.projectId`) · `expo-dev-client ~6.0.21` · **BUG-259** (root `preinstall` → `npx only-allow pnpm`) · `RECORD_AUDIO` stripped (`microphonePermission: false`; `introspect` confirms the plugin now emits a manifest REMOVE directive). 5 files, 109(+)/2(−). **AUDITED: PASS.** Mobile suite 1379 / 36 / 0 (= baseline), both typechecks clean. 🔵 **OWED FROM HANS: `eas build --profile development --platform android` — FROM `artifacts\kiwi`.** ⚠️ First attempt ran at the repo ROOT and generated a stray root `eas.json` (delete it; `artifacts/kiwi/eas.json` is the real one). *Beneath:* ✅ **`f0b392c` — PUSHED September 13 (`fd5ddfc..f0b392c`, 20 objects, from Hans's terminal). HEADER DEVICE-VERIFIED (Hans: *"pass, I see the new logo"*).** `f0b392c` = the Deep Kiwi mark wiring (MOBILE lane): `app.json` icon / splash image / adaptive icon / favicon → the new asset set (splash background and `resizeMode` untouched), `HomeHeader.tsx` text wordmark → the 28 px mark image with `accessibilityLabel="Kiwi"`, nine assets committed, `HomeHeader.test.ts` (3 guards, break went 3/3 red, restored). **AUDITED: PASS.** Mobile suite **1379 pass / 0 fail / 36 suites** (one run; no prior mobile baseline is recorded here — the three new tests imply 1376 before), typecheck clean. 🔴 **The `app.json` half is UNVERIFIABLE until a real build exists — Expo Go shows its own icon and splash. Only the header is device-checkable today, and that pass is owed.** ⚠️ Two product calls chat-Claude made in the prompt, reversible: header is MARK-ONLY; splash stays sage. *Beneath:* ✅ **`fd5ddfc` — PUSHED September 12, and it is what is RUNNING on Cloud Run.** `fd5ddfc` = the Dockerfile Prisma-path fix + the DEPLOY.md probe correction; **AUDITED by chat-Claude against the bytes on disk before the push (staged through the device bridge): `PRISMA_NM="$(dirname "$(dirname "${CLIENT_DIR}")")"` with a `test -d` guard BEFORE any `mkdir`/`cp`, copy source `${PRISMA_NM}/.prisma/client`, comment rewritten to state the real relationship. CC's negative control on the old path returned FALSE and its `require.resolve` landed exactly where chat-Claude measured.** Suites **2047 = 2045 pass + 0 fail + 2 skip** (= baseline; a path fix adds no test — the `test -d` IS the guard, in the Dockerfile), typecheck clean. ⚠️ **One disclosed local deviation: filling the scratch tree, CC skipped the ~1.2 GB `*.tmp*` Windows leftovers with `find … ! -name "*.tmp*"` instead of the bare `cp -r`; the derivation, the guard and the source dir were the Dockerfile's exact expressions, and on Linux no `.tmp` files exist.** *Beneath:* ✅ **`8183f5d` — PUSHED (`aa13fc6..8183f5d`, 32 objects, confirmed from Hans's terminal)** = WS9A Phase 1: root `Dockerfile` + `.dockerignore` (multi-stage, `node:24.21.0-bookworm-slim`, `prisma generate` inside the image, hand-assembled runtime tree) · `artifacts/api-server/DEPLOY.md` · the two D-WS9-231 pages served at the host ROOT (`/reset-password`, `/verify-email`, plain HTML in `src/pages/`, mounted before `/api`, no auth, `no-store`) · **BUG-229 FIXED** (`start` builds first; measured `dist/` moving 2026-05-31 → 2026-09-12) · **BUG-257 FIXED** (`passwordChangeLimiter` on `verify-change`). Suites **2047 = 2045 pass + 0 fail + 2 skip**, typecheck clean, both under a portable Node 24.21.0. Five deliberate breaks, each red and restored byte-identically. ✅ **THE IMAGE NOW BUILDS AND RUNS — every Linux-only watch item (engine download, the `openssl` need, the hand-assembled runtime tree) held on the first real Cloud Build. `/api/readyz` answering `ready` FROM CLOUD RUN is the proof that `.prisma/client` landed exactly where `require.resolve` looks; the audited path fix is what made that true.** *Beneath that:* **`d744bb1`** — `wizard.candidate.expand` **v9**; ✅ **RESEEDED September 12 (1 bump, expand v8 → v9, as predicted).** **Device: everything since `4222db4` is device-UNPROVEN; the `'most'` ceiling is device-untested — test it at a 45 or 60 cap, not 30 (at 30 the shelf is six and the ceiling has nothing to act on).**

**Knowledge:** **1.60 MB of 2.00 MB** after the September 12 archive pass. ⚠️ **It was 1.74 MB and REFUSING WRITES: the ~01:50Z writes of both logs failed silently after `claude/kiwi_meal_selection_arc_scope.md` landed, and the mirror was synced anyway — so the MIRROR RAN AHEAD OF CANON by D-WS9-238 and the BUG-255 challenge addendum (canon at 01:06Z/01:13Z, mirror at 02:00Z). Recovered September 12 midday by rebuilding both logs from the mirror body; canon and mirror agree again.** The pass: 94 ✅ entries below BUG-225 → `kiwi_bug_log_ARCHIVE_2026-09-12.md` (mirror, verbatim, §24.10) · the 73 KB bug-log pointer-line narration and the 30 KB deferred-log pointer-line narration → the dated archives (mirror, verbatim) · BUG-256 amended with CC's measurement · the bug log's `## OPEN` header renamed (it held every status — §A(3)).

🔵 **IN FLIGHT — INSTRUCTED, NOT LANDED (§26.4; one line per item; delete each as it reports):**
- 🔵 **INSTRUCTED, NOT YET RUN — THE BUG-270 LANE (`kiwi_cc_prompt_SERVER_bug270_pathblind_scheduler.md`, chat-only; SERVER; one hard STOP before any catalog row moves).** Phase A re-establishes the fact from the code (the path field, the default path, every consumer that walks steps serially) · Phase B: schedule BASE + DEFAULT path only, filter in ONE place so every consumer inherits it, check that excluding bought steps cannot orphan a `parallelGroup` token · Phase C: `d235 --dry` over the catalog with the fix — meals moved, mean/p50/p90/max, **the honest 30/45/60 shelf for the first time**, the 95 supplement meals’ corrected distribution, top 5 by shortening opened by eye — written to `restamp_pending.json`, **then STOP: the re-stamp is 1,349 dishes of shared catalog data and Hans’s call.** 🔴 **The prompt restates §27.6 as a licence to REFUSE and STOP, never to CONTINUE, after tonight’s override.**
- ✅ **REPORTED + AUDITED — THE 30-MINUTE SUPPLEMENT LANE (September 14 evening). Step 0 PASS (rest/hold written, invariant restored). The run: 95/95 generated for $4.85, real dinners, correct splits — and it MISSED the 30-minute target because `shortcut_used` never reached the prompt (input defect), and its numbers are INFLATED by BUG-270. Hans’s keep/trim/rerun call on the 95 is DEFERRED until BUG-270’s Phase C. THE LANE OVERRODE AN EXPLICIT STOP and disclosed it fully — ruled and recorded on D-WS9-240 item 6 and in process debt.** Recorded on D-WS9-239, D-WS9-240, BUG-270 (new). Lane CLOSED. *What it was when instructed:* (`kiwi_cc_prompt_SERVER_30min_supplement.md`, chat-only; one SERVER lane, NO STOPS, self-aborting).** **Step 0 writes the rest/hold change set** (4 meals / 8 rows, measured and audited, Hans ruled GO) **— first, because Phase 3’s invariant "every stored time equals what the shipped scheduler derives" is currently FALSE on those 4 meals and §2’s acceptance depends on it being true.** Then **D-WS9-240 item 6**: the 95-dinner target list generated through the REAL generation path so the new dinners are born with the `firstDependent` contract. 🔴 **Protections: a $60 in-script breaker · STOP if the 5-meal projection exceeds $45 · resumable · an EARLY ABORT if fewer than 5 of the first 10 land at ≤ 30 DERIVED minutes** (that would be a design problem, not a spend problem). **Acceptance is the DERIVED time, never the estimate; a 34-minute meal is a good meal per Hans’s ruling.** **The hand-check is a seeded random 10 PLUS the top 3 by largest claimed saving — sorting on magnitude is now standard after BUG-266.** ⚠️ **The generation SEAM is deliberately not named in the prompt: chat-Claude has not verified it, and naming an unverified mechanism is the error this week’s process debt records three times. CC finds it and states it.**
- ✅ **REPORTED + AUDITED PASS — THE SERVER LANE (`005b8f2` rest/hold · `dca04a9` BUG-267 · `306326e` BUG-265) AND THE MOBILE LANE (`2c983d8` BUG-264 · `41caf58` D-WS9-241 D), run side by side September 14. Recorded on D-WS9-239, D-WS9-241, BUG-264/265/267, and BUG-269 (new). Both lanes CLOSED.** 🔴 **The mobile lane caught the three-body-shape defect that would have rendered `"ai_failed"` to users; the server lane disclosed that my prompt’s test-count claim was false and explained why (1c’s side effect correctly reversing itself).** 🔵 **Device pass owed on the mobile pair:** the Welcome mark at 96 pt on a 3× screen, and the four spend-guard surfaces with the cap tripped. 🔵 **One item reported and NOT fixed, `app/**` being untestable: `wizard-results.tsx` renders its own title *"Kiwi got distracted. Try again?"* above the server’s body — fix-on-next-mobile-touch, with a device pass that can see it.**
- ✅ **D-WS9-239 PHASE 1c — ALL FOUR PHASES REPORTED + AUDITED PASS. THE ARC IS COMPLETE ON SHARED DATA; THE LANE IS CLOSED except for two named follow-ons (the rest/hold refinement at $0, and 1d).** Phase 3 wrote 700 meals / 3,530 step rows; the 5 BUG-266 exclusions are recorded BY ID on D-WS9-239; the revert is one dry-by-default command. **Nothing further is owed from that CC chat.** *What it was:* Phase 0 refused variant A on CC’s counter-measurement and ruled variant B at N = 5 · Phase 1 landed `6d0f5d9` · **Phase 2 tagged the whole catalog to disk for $13.78 and wrote NOTHING** · 🔵 **PHASE 3 (the catalog write) is GIVEN THE GO pending Hans’s call — it touches D-WS9-230’s shared-catalog carve-out, which is his ruling to make. Go-ahead delivered chat-only with five specifics, of which the load-bearing one is: PROVE the running module still matches `catalog_tags.json`’s meta before the first write, and assert the Anthropic client is never constructed.** Full outcome on D-WS9-239. Phase 2’s six added specifics: shipped-module derivation (proved by printing `ADJACENCY_MAX_MINUTES` at startup) · a **$20 cumulative breaker IN THE SCRIPT** · **resumable** (append per dish, skip by `dishId`) · projection from Phase 0b’s MEASURED per-dish cost, not from a cache assumption · **early abort at 50 dishes** against Phase 0b’s issue-class rates · **two hand-check samples reported separately** (the seeded random 20 is the acceptance sample; up to 5 `adjacency_override` fires are a look at the new code path and do NOT enter the acceptance rate). *What it was when delivered:* (`kiwi_cc_prompt_SERVER_dws9_239_phase1c.md`, chat-only; the SAME CC chat; self-check HEAD `b71ec2d` + `gen_b_241.json` + `tags_b_sonnet_240.json` present; THREE hard STOPs; cap $25, expect ~$17).** **Phase 0 ($0):** re-derive the existing `gen_b_*` and `tags_b_*` outputs at N ∈ {3,4,5,6,7,off} and report the curve — how often the adjacency rule fires, what it fixes, and 🔴 **how many LEGITIMATE windows it destroys** (CC is asked to kill the proposal if the net is bad). **Phase 1:** implement at the ruled N, `adjacency_override` report-only, commit — **no prompt change, so no reseed.** **Phase 2 (~$17):** the Encoding-B tagger over the catalog, pre-filtered to dishes with a usable unattended step, output to `catalog_tags.json`, a seeded random 20 hand-checked, the population shape including **how many meals cross 30/45/60 downward** — **writes nothing.** **Phase 3:** batched writes from that file, pre-write dump, verify-30, a stated revert command. **Unblocks D-WS9-240 item 6, the 30-minute UAT supplement.**
- ✅ **REPORTED + AUDITED + DEVICE-VERIFIED + PUSHED — THE AUTH-CORE LANE, D-WS9-241 A + B + E (`dd472c5` → `2c69e3d` → `431f935`). Recorded on HEAD, on D-WS9-241, and on BUG-258 / BUG-261 (both CLOSED). LANE CLOSED; nothing mobile or server is running.** 🔴 **One finding rides out as BUG-267: `PATCH /me/profile` has no SMS-without-phone check, so consent can survive a phone change and number B inherits a consent given for number A. Ruled (two rules, both evaluated against the ROW AFTER the patch); fix-on-next-touch of `routes/me.ts`.** *What it was when instructed:* (`kiwi_cc_prompt_AUTHCORE_dws9_241_abe.md`, chat-only; a NEW CC chat, beside 1c Phase 2, file-disjoint per §29.1).** **Three commits, in order, NO STOPS: E** web token storage → `sessionStorage` behind a `Platform.OS === "web"` branch with every read/write wrapped so a throwing or blocked storage reads as "no token", native path byte-unchanged · **B** a 10-second `AbortController` on the bootstrap `/auth/me`, 401 cascade unchanged, timeout/network/5xx → a *Can’t reach Kiwi* screen with the **token KEPT** and Try again / Sign out, `readToken()` throwing → caught → Welcome · **A** `phone` + both consents on the SIGNUP wire (one write; SMS consent without a phone → 400; the phone rule SHARED with `PATCH /me/profile`, never re-implemented). **One commit per ruling so a morning device break bisects by commit** — D-WS9-230’s checkpoint refinement applied to an unattended run: the variable is isolated without spending the overnight window on a stop. **Three hazards handed to the lane as CHECKS, not assertions:** `useAuthMe`’s react-query retry policy (a default 3-with-backoff makes a 10-second abort surface in forty-plus, so the bound is pinned and the prior value reported) · `AuthContext`’s `await readToken()` inside an effect never sets `storageRead` if it rejects — which IS the third failure path, literally · 🔴 **the failure screen must cover a cold start on ANY route, not just `/`, and `SessionGate` must return early on a bootstrap failure or it evicts the screen to sign-in** (token kept, `user` null, `isLoading` false). ⚠️ **Corrects this project’s own record: `app/(auth)/sign-up.tsx:84` calls `signup(email, password, firstName, lastName)` — the client does not even SEND phone or the consents, so A is two defects stacked, not one.** Device script owed from chat-Claude at audit; the lane reports the steps it wants rather than writing one (§26.5).
- ✅ **REPORTED + AUDITED — D-WS9-239 PHASE 1b-ii, both phases (`b71ec2d`, LOCAL). Recorded on HEAD and on D-WS9-239. THE GATE IS MET.** Fresh seed 241: **0 of 22 tagged dishes MAJOR**, 6 minor (1–4 min), 0 missed, 19 no-overlap. Seed 240 re-run: MAJOR 7 → 2, dishonest minutes 73 → 18. Zero `null` in 184 declarations; `missing_window` did not rise (1 and 1, both pre-existing phase mis-tags); the two "nothing after this" escapes were refused. Lever on 241: guarded mean 9.45 / **trusted 8.65 — the two have converged**, which is the real tell. $1.58 this phase, $3.42 cumulative. ✅ **CC verified the reseed precondition from the `AIPromptVersion` ROWS rather than from Hans's paste (he pasted the prompt text by mistake) — better evidence than the transcript, and the shape to repeat.** 🔴 **One class survives and PROSE HAS NOW FAILED IT TWICE — the setup-step skip (a 1–5 min unattended step declared with dependent `i+2`, so its own continuation rides its kickoff): every wrong dish on both runs, ~30%, with the prompt naming those exact examples. RULED: fix it in the DERIVATION, not the prompt — its errors run in the SAFE direction and it needs no reseed. That is 1c Phase 0/1.** ⚠️ **Process debt (below): the Bash tool's heredocs collapse `\\` → `\`, which silently no-op'd several of CC's perl/node one-shots earlier in the lane — it verified every landed edit by char codes instead.** What 1b-ii was: Removes `null` from the model's vocabulary (omit on the last step; derivation refuses `null` → `null_declared`, no token; zod stays tolerant so a stray `null` never fails a save); replaces one sentence in all three prompt surfaces; commit → **Hans reseeds (expand v10 → v11, finalize v8 → v9), restarts, pastes the bump lines** → seed 240 re-run (before/after) + FRESH seed 241 (the gate, ≤ 5% MAJOR ≥ 5 min, minor reported) → STOP. ~$1.60. **1c only after.**
- ✅ **REPORTED + AUDITED — D-WS9-239 PHASE 1b, both phases (`a9d2061`, LOCAL, NOT PUSHED). Recorded on D-WS9-239.** Phase 1 PASS: suite 2161 = 2159 + 2 (+36), catalog dry run moved 0, every break red; the one break that stayed green got a fixture that reaches its branch (§27.5, CC's own catch). Two CC additions accepted (`isUnattended` exported from 1a's file — visibility only; `declaredCount` gate so untagged dishes stay silent). **Chat-Claude corrected:** the finalize seam is `finalizeOneMeal` (not exported, per-meal `dishSteps`), so the gate probe drives `runAICall` with its exact arguments behind a prisma proxy that swallows `LLMCallLog.create`. Phase 2 (the gate): **60% wrong-window (12/20 tagged, v8, seed 240) — FAIL; $0.75; root cause the `null` inversion, 9/9; 4 minor setup-step cases; the plausible half places the big windows correctly; every one-early went safe; rule (b) rescued two; anomaly guard fired once.** What it was: Phase 1: port Phase 0b's `deriveFromWindows` to `src/lib/parallelGroupDerive.ts`; `WizardStepSchema` + `WizardOutlineStepSchema` gain `firstDependent` (described; `parallelGroup` never model-written); derivation applied in wizard activation, the expand outline→timing adapter and store-fill; wizard finalize + expand bodies and the store-fill prefix replace the prose parallel-windows guardrail with the structural rule (expand v9→v10, finalize +1 at reseed); `toStepShape` emits the column; catalog dry run must still move 0 → commit → STOP. **Hans: `prisma:seed` with the dev server stopped, paste the two bump lines into that chat, restart.** Phase 2: the fresh-20 gate on seed 240 through the REAL finalize seam (dry run, ≤ $5): wrong-window ≤ 5% gates 1c; text/structure disagreement counted separately → STOP.
- ✅ **REPORTED + AUDITED + PUSHED — THE BUG-235 CLIENT LANE (`9ef29e2`). Recorded on HEAD and on BUG-235. Lane CLOSED.** Suite **1383 = 1379 + 4** (one run), typecheck exit 0, break red + sha1 identical either side, both existence shapes zero, status hash unchanged. **Typed routes needed no commit** — `.expo/types/router.d.ts` is gitignored and Metro regenerated it on file add (precedent `ce491ca`); neither `_layout.tsx` changed. **Three disclosed deviations, all ACCEPTED by chat-Claude:** a fallthrough *"Something went wrong. Please try again."* for a non-`ApiError`/non-`ApiNetworkError` throw (**keep it — the ruling enumerated three classes and a fourth would otherwise render nothing; an `ApiSchemaError` on a malformed 200 is the real case**) · the `inFlightRef` synchronous latch beside `submitting` (**keep — state alone cannot stop two taps in one tick**) · array-tolerant token read (**keep**). 🔵 **Device script owed — its headline item, the real emailed link, is blocked on BUG-224's Resend DNS.**
- ✅ **REPORTED + AUDITED — the AUTH-SURFACE PHASE 0 (read-only, beside 1a). Audit PASS; its six refutations and the five rulings it produced are D-WS9-241. Lane CLOSED.** The five defects it inventoried are now: BUG-261 + BUG-258 + the web branch → the auth-core lane (D-WS9-241 A/B/E); BUG-235 → the client lane above (C); the spend-guard copy gap → the error-copy lane (D).
- ✅ **REPORTED + AUDITED — D-WS9-239 PHASE 1a (`f543183`, LOCAL). Recorded on HEAD and on D-WS9-239 (1a outcome, CC's contract calls, the two findings that reshape 1b, candidate defects, and a new 1d follow-on: the four text-mode generators + the mobile save payload, after 1c). That chat is CLOSED.** What it was: Scheduler honours `parallelGroup` (contiguity · same-component rest/hold rule · path rule · invalid-ignored · anomaly guard emits the shorter of tags-honoured/tags-ignored · `ignoredTags` returned, module stays logger-free) + every carrier (`mealTiming` select/map **and `dishTotals` from the scheduler**, `cookingSequence` map, `MaterializeMealStep` + 4 creates, **`readPreservableSteps`**, `mealFork.STEP_COPY_FIELDS`, `wizardActivation`, `storeFill`, `me.ts stepItemSchema` + the three TEXT-mode AI schemas + D-WS7-012's `.max(40)`) + the `d235-derive-meal-timing` harness. 🔴 **HELD OUT ON PURPOSE: the two TOOL-mode schemas (`WizardStepSchema`, `WizardOutlineStepSchema`) and every prompt body — a field in a tool schema is a field the model fills unguided; they ship in 1b with instructions. CC is asked to verify that hazard and say if I am wrong.** **Acceptance is that the lane is INERT: all ~1,202 stored times byte-identical.** Two pinned tests change meaning (`me-save-canonical.test.ts:389`, `mealBuilder.test.ts:570`).
- ✅ **REPORTED + AUDITED — the D-WS9-239 Phase 0 AND Phase 0b (SERVER, read-only; $0.87 + $1.08 = $1.95 total). Both recorded on D-WS9-239. That chat is CLOSED — 1a is a fresh one.** Probes untracked at `artifacts/api-server/scripts/ws9-239/`, outputs under `scripts/output/ws9-239/` (gitignored) — `handcheck_b.md` and `trust_read_b.md` are the dish-by-dish evidence if the ruling needs it. **Audit notes against chat-Claude, recorded on the entry:** the "~12 min" relayed into the Phase 0 prompt as the regex estimate was the UPPER bound; the "bad tags only make the number more conservative" premise was false for semantically wrong tags. **CC candidate defects, none minted (all latent until tags exist — Phase 1 scope): silent strip in every AI step schema · `readPreservableSteps` / `mealFork.STEP_COPY_FIELDS` would drop persisted tags · `mealTiming.dishTotals` Σ-serial · dual-path cross-path groups (interaction with BUG-121) · the 79→82 meal-total anomaly · `cookNow.ts` is a dead file carrying the D-WS7-010 `parallelGroup` shape.**
- ✅ **REPORTED + AUDITED + DEVICE-VERIFIED + PUSHED — the BUG-260 MOBILE lane (`9b9b01a`). Recorded on HEAD and BUG-260. Lane CLOSED; nothing mobile is running.** Hans's one open look from the mark lane — the LAUNCHER icon — is still unanswered (his item-6 answer described the splash); his item-3 note *"the splash page and old logo"* is unexplained and is asked about in chat. 🔵 **DELIVERED, NOT RUN — the Prisma `onlyBuiltDependencies` step (Hans, in chat): add `@prisma/client` · `@prisma/engines` · `prisma` to `pnpm-workspace.yaml`, dev server STOPPED, `pnpm install` from the root, confirm `prisma generate` ran (the "Ignored build scripts" box gone), commit the one file, restart. Safe now because the server chat is idle.**
- ✅ **REPORTED + AUDITED — the spend-guard SERVER lane, Phases 0 and 1 (`6ff7d8c`, LOCAL). Lane CLOSED.** ✅ **Its follow-up landed too — `da8969b`, BUG-262 + BUG-263 both FIXED and audited. THE SPEND-GUARD WORK IS COMPLETE — code, Cloud Run env and the GCP alert all landed September 13.** 🔵 **One item ruled FIX-ON-NEXT-TOUCH, not commissioned:** CC's own proposal that `logCannotEvaluate`'s pass-through surface at boot as `readSurface: ok|missing` (a `typeof prisma.lLMCallLog.aggregate === "function"` probe against the production singleton, in the same `info` line). **Chat-Claude's ruling: yes, worth having — the guard's failure mode is silence, and this turns "someone passed a stub and everything passed through" from invisible into one boot line. But the production singleton is a real `PrismaClient`, so today's risk is zero and it does not justify a lane. It rides with the next server lane that opens `spendGuard.ts` or `app.ts`.** **Accepted with note, not defects:** `ai_disabled` returns `Retry-After: 300s` rather than UTC midnight (CC's deviation, and it is RIGHT — a kill switch has no natural expiry and midnight would be a lie) · **fail-open on a guard DB read error** (CC's own reasoning holds: `requireAuth` does a per-request DB read (BUG-234), so a real outage kills the request before the guard — ⚠️ **the residual is connection-pool exhaustion under exactly the load a drain attack creates, where auth may get a connection and the guard may not; the Anthropic console cap is the backstop and this is recorded, not fixed**) · the file set widened by `wizardExpansion.ts` / `wizardFinalize.ts` (**necessary** — they folded every failure into `meal_failed:…`, which would have collapsed a guard refusal back to 502), `dishBuilder.ts` + `/builder/parse-dish` (consistency), `promptRegistry.ts` (type surface) · **CC declined the end-to-end `build-plans` A/B with correct reasoning and chat-Claude does not want it run**: that route is ONE `streamPlanCandidates` call, so the guard adds one ~63 ms check (≈5 ms co-located) inside a 9–22 s Anthropic call whose run-to-run variance is seconds — the A/B cannot resolve the effect and would spend real tokens to say nothing. ⚠️ **This also corrects chat-Claude's premise in the go-ahead: a wizard flow is not 8–15 guard checks; fan-out in expand/finalize is `Promise.all` at both levels, so the guard costs 1–2 checks of WALL CLOCK per request, not 8–15.** Measured: guard both-checks-on p50 **63.5 ms** / p95 116 ms from Hans's machine (bare `SELECT 1` is 29.9 ms — the cost is two round trips, `EXPLAIN ANALYZE` execution is 1.9 ms), ≈**10–20 ms co-located on Cloud Run**; env unset = 0.0 ms. Below the 200 ms bar → **no per-request memo built, correctly.**
- ✅ **RULED — the spend-guard COPY gap is D-WS9-241 (D): for `spend_cap_user` / `spend_cap_global` / `ai_disabled` the SERVER copy wins on every client surface, keyed on `reason`; other 502/429/503 keep local copy. Its own mobile lane (the error-copy lane) runs after the auth-core lane. ⚠️ The earlier lines here ("shows NO copy", "wizard/builder/prep-week are fine") were REFUTED by the auth Phase 0 — generic wrong copy, and prep-week + the builders override too.** The spend-guard build record that follows is unchanged: Phase 0's measurements and the ruled values are on `kiwi_go_live_todos.md`'s spend row — **two doors only, every AI route authenticated, both indexes already present (no migration), and the first real per-user spend profile this project has had.** Three findings changed the design: `no_api_key` DOES write a log row (the guard must not mirror it) · **`errorHandler` is not a viable single mapping spot** (it honours only thrown 4xx and discards the body), so a shared `aiFailureStatus(reason)` helper is applied at each `status(502)` site instead · the reason-collapsing routes were ruled — import/builder/grocery thread it, **scale/macros/conversionFallback deliberately do not, because under a cap a graceful degradation IS correct behaviour.** ⚠️ **Phase 1 also owes a LATENCY measurement chat-Claude added: two DB round trips per AI call × 8–15 calls in a wizard flow, on the most latency-sensitive path in the product; the sanctioned mitigation if it is material is a PER-REQUEST memo, never a process-global cache (Cloud Run runs more than one instance).** Phase 0 is READ-ONLY: inventories every door to Anthropic (chat-Claude read two — `runAICall` and `streamPlanCandidates`, which does NOT go through `runAICall`), how `AICallFailure` reaches HTTP per route, which AI routes are unauthenticated, the `LLMCallLog` indexes, and MEASURES calls/user/day and daily user-attributed spend from the live log to propose the defaults. **Stops after the report; Phase 1 (`src/lib/spendGuard.ts` at the top of BOTH doors — kill switch · global daily ceiling on `userId IS NOT NULL` rows · per-user daily call count; 429/503; no `LLMCallLog` row on refusal; DEPLOY.md env rows + its stale probe values fixed) goes on chat-Claude's go after audit.** Design rationale is in the prompt's §3; the keying is `userId`, never IP — the IP limiters are ONE global bucket on Cloud Run until `TRUST_PROXY_HOPS` is measured.
- ✅ **REPORTED + AUDITED — the READ-ONLY web-export probe (row 3b). Verdict SMALL REWORK; recorded on the roadmap row and as BUG-258's third path. `git status` md5-identical before and after; lane closed.**
- ✅ **REPORTED + AUDITED — the EAS-COMMIT lane (`aef6c04`, pushed September 13). Recorded on HEAD, BUG-259, BUG-260. Lane closed.** Two findings ride out: **BUG-260** (`expo-location`'s location permissions with zero import sites; six more dead native packages) and **a Hans decision: add `@prisma/client` / `@prisma/engines` / `prisma` to `pnpm-workspace.yaml`'s `onlyBuiltDependencies`** — today `prisma generate` is SKIPPED on every local install (the Dockerfile generates explicitly, so the deploy is unaffected; a fresh clone does not work until someone runs it by hand). Chat-Claude's read: add it, with the standing rule that the dev server is stopped before any `pnpm install` (a regenerate against a locked engine DLL is what produced the 1.2 GB of `.tmp` leftovers).
- ✅ **DONE — the EAS Android dev build. Built, installed, running on Hans's phone against Metro at `192.168.0.50:8081`; icon verified from the APK; header mark verified on the dev build. Expo Go is RETIRED for device testing. Recorded on `kiwi_go_live_todos.md`.** 🐛 **BUG-261 minted from the same pass:** sign-up collects phone + two consents and `signup()` sends none of them (the WS6 consent columns exist; the client wire was never built). ⚠️ Hans's *"logout used to reach the welcome screen"* is BUG-239's RULED fix (dead session → sign-in), not a regression; welcome is now a cold-start-with-no-session screen.
- ✅ **REPORTED + AUDITED — the MOBILE Deep Kiwi mark wiring (`f0b392c`, pushed September 13; header + icon device-verified). Recorded on HEAD and on `kiwi_go_live_todos.md`. Lane CLOSED; nothing is running.** ⚠️ Audit note against chat-Claude, not CC: the prompt's restore check ("`git diff` empty on that path") was unfit for a file the commit itself modifies; CC said so and verified by diff inspection instead. The right instruction is a pre-break / post-restore HASH — §27.4, process debt below.
- ✅ **REPORTED + AUDITED — the WS9A containerize lane (`8183f5d`). Recorded on BUG-229 / BUG-257 and here. Lane NOT closed: one fix rides (below).**
- ✅ **REPORTED + AUDITED — the Dockerfile fix (`fd5ddfc`, LOCAL). The defect: `cp -r "${CLIENT_DIR}/../.prisma/client"` named a directory that does not exist — `.prisma` is a sibling of `@prisma` inside the same `node_modules`, two levels above the package dir. Measured by chat-Claude on a clean pnpm 10 install (`require.resolve('.prisma/client/default',{paths:[CLIENT_DIR]})` lands two levels up; with only the one-level dir present it throws `MODULE_NOT_FOUND`), confirmed by CC's negative control. `set -eu` meant the image build would have aborted there — loudly, but blocking the first deploy.** ⚠️ **Not a BUG entry: found and fixed inside its own block (§28.1).** ✅ **DEPLOY.md's wrong reason for the second probe call is corrected in the same commit** (`gcloud run deploy` DOES take `--startup-probe`/`--liveness-probe` inline; the second call stays because `PUBLIC_APP_URL` is unknown until the service exists). ✅ **PUSHED by Hans September 12.**
- ✅ **RUN AND REPORTED — `kiwi_device_script_ws9a_first_deploy.md` (12 items).** Landed: the four secrets into Secret Manager · a DEDICATED BUILD SA after the first deploy took a 403 (`kiwi-build@kiwi-prod-508416.iam.gserviceaccount.com`, `roles/run.builder`, passed with `--build-service-account` — the default compute SA was NOT given build rights, see `kiwi_go_live_todos.md`) · **`gcloud run deploy --source .` SUCCEEDED: the image built in Cloud Build and the container booted** · service URL `https://kiwi-api-166146829159.us-east4.run.app` · `PUBLIC_APP_URL` + both probes set, revision **`kiwi-api-00002-bz5`**. **Acceptance, all green from Hans's terminal:** `/api/healthz` → `200 {"status":"ok"}` `no-store` · `/api/readyz` → `200 {"status":"ready","latencyMs":7}` · `/reset-password` → `200 text/html` `no-store` 4814 B · `/verify-email` → `200 text/html` 3782 B. **And from chat-Claude in the built-in browser, against the deployed service:** `/reset-password` with NO token renders “Link not valid” and fires no request · `/verify-email?token=junk` renders the invalid state · the reset form with a junk token POSTs to `/api/auth/password-reset/confirm` ON THE DEPLOYED SERVICE, takes the 400 and renders invalid with the `kiwi://reset-password?token=…` app link — **the browser-completable reset D-WS9-231 was ruled for works end to end in production.** 🔴 **THREE ITEMS OF THIS SCRIPT ARE STILL OPEN, all on Hans: item 7's REAL cold-start number (`latencyMs: 7` is a WARM read on a live revision — it does not test `/readyz`'s 2 s bound against a SUSPENDED Neon, which is the question the item was written to answer), item 10 (a real login — it needs his test-account credentials; chat-Claude does not handle credentials), and item 12's two-network `TRUST_PROXY_HOPS` measurement (needs his phone on cellular; the variable stays UNSET until measured).** ⚠️ The EMAILED round trip remains untestable — the Resend DNS verification at GoDaddy is still outstanding (BUG-224).
- ✅ **REPORTED + AUDITED — the READ-ONLY intra-dish measurement. Recorded on BUG-256 (second amendment) and D-WS9-239. Probes untracked at `scripts/ws9-sched-order/q5*.ts`. Lane closed.**

**Decisions owed by Hans — pointers only; the evidence is on the entries:** ✅ **THE LAUNCH SHAPE — RULED September 12, D-WS9-240:** first publish = hosting + distribution · WS9B store-bought toggle · the meal-selection arc **Blocks 0–2** (Block 3 + Cookbook browse post-launch) · meal images · favorites/ratings · the 30-minute supplement · a spend ceiling · the web probe soon; ship FREE; OAuth + Stripe fast-follow; freshness 2+ months after; latency after; polish ongoing. ✅ **THE THREE ROWS RULED September 13: 3a IN · 4 OUT · 8 = FIRST fast-follow, *"top priority before I really start marketing"* — with a friendly "online grocery ordering is coming soon" STUB at launch (modifies D-WS9-099).** ✅ **THE TWO-TIER SEQUENCE + ~OCTOBER 15 CHECKPOINT — ACCEPTED by Hans September 13; his posture: the date slips before the product does.** ⚠️ Hans read the estimate as "50 blocks, 7 a week" — **the WORK estimate is 18–24 blocks; 20–25 was the capacity guess.** The checkpoint measures the real rate. ✅ **`claude/kiwi_meal_selection_arc_scope.md` §8 — RULED by the same message (Blocks 0–2 IN); the row is written (roadmap 2b).** ✅ **D-WS9-239 RULED (a) September 12 — IN, as the server block right after WS9A lands and BEFORE the UAT supplement; two-sautés-at-once DECLINED as scope; the scheduler infers no overlap, it honours what the generator declares.** ✅ **D-WS9-238 RULED September 14 — CATALOG FIRST, AI FALLBACK.** ⚠️ **One measurement is attached and owed BEFORE the build, not before the ruling: the per-generation cost and wall clock of an invention-heavy plan (0 / half / all slots missing the catalog), and the per-user-cap headroom that leaves.** ✅ **BUG-205 RULED September 14 — option (c), P1 → P2, and the treatment is one conditional footnote in the WS9B toggle lane.** 🔴 **OWED FROM HANS, IN ORDER OF WHEN THEY CAN BE MADE:** **(1) after BUG-270’s Phase C report: GO / NO-GO on the catalog RE-STAMP** (1,349 dishes, the carve-out) **and, on the same honest numbers, KEEP / TRIM / REVERT-AND-RERUN the 95 supplement meals** — the rerun would carry `shortcut_used` on the wire, ~$5. **(2) `git push`** for `41a263d` + `d3bcfdc`. **(3) run the `EMAIL_FROM` fix and the reset-email test.** **Operational, not decisions:** the cold-start `/readyz` number (tomorrow morning) · the two-network `TRUST_PROXY_HOPS` curl · whether `scripts/ws9-239/` ever gets committed.

🔴 **NEXT, in this order:**

1. 🔴 **Hans, in this order:** ✅ (a) the Prisma `onlyBuiltDependencies` step — DONE, `d7cde7a` pushed; (b) ✅ 1a and BUG-235 both PUSHED (`d7cde7a..9ef29e2`); ✅ **(b2) the BUG-235 lane's report ARRIVED and audits PASS; its `verify-email` / `SessionGate` answer became BUG-265;** ✅ (b3) 1c Phases 0–2 and the auth-core lane ALL reported and audited; ✅ (b4) EVERYTHING IS PUSHED — HEAD `7608ebf`, nothing local-only; ✅ (b5) Phase 3 RULED GO and LANDED — the catalog is written; ✅ (b6) BUG-268 FIXED (`7608ebf`) and THE AUTH-CORE DEVICE PASS RAN AND PASSED — 🔵 **its one residual is the NATIVE deep-link initial-URL question, which needs `adb` (standalone platform-tools; `Test-Path "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe"` says whether it is already there). Not launch-gating;** ✅ (c) *"old logo"* is ANSWERED — it was the WELCOME screen, not the splash (**BUG-264**); 🔵 **still owed: one look at the Android LAUNCHER icon** (a different surface again, and the only brand check no screenshot has covered); (d) ✅ `Claude outputs/` is gitignored (`9b9b01a`). **Then the three WS9A leftovers below, none of them code:** (a) **the cold-start check** — hit `/api/readyz` once Neon has auto-suspended (a few idle hours, or suspend the branch in the Neon console) and send back the `latencyMs`, or the `503 db_unavailable`-then-`200` pattern if that is what happens; **that number is what decides whether the 2 s bound rises**, and until it exists the bound is untested against the case it exists for. ✅ (b) **a real login against the deployed API — DONE September 14, a user object came back, so the deployed service authenticates against production Neon end to end. Script item 10 CLOSED.** (c) **the two-network `TRUST_PROXY_HOPS` curl** — cellular vs home wifi (script item 12); leave the variable unset until then.
2. ✅ **WS9A DEPLOY LANE — DEPLOYED September 12 (`8183f5d` + `fd5ddfc`, both pushed, both live). The CC lane is CLOSED and nothing is running.** 🔴 **Two named follow-ups ride out of it:** `artifacts/api-server/DEPLOY.md` still documents the INVALID probe values (`periodSeconds=2` with `timeoutSeconds=3` — Cloud Run rejects that outright; the live service runs `periodSeconds=4`), and the Cloud Run RUNTIME identity is still the default compute SA (`kiwi-run@` is a go-live row, not a today row). What the lane built, for the record: Containerize (multi-stage image; `prisma generate` INSIDE the image, no `binaryTargets`; Node pinned — none is today; no Dockerfile exists in the repo, verified September 12) → Cloud Run (Google Cloud account exists) → **the D-WS9-231 web page, server-served** (password-reset completion + email-change verify — the reset must be completable in a BROWSER; the app deep-link is the later enhancement on the same URL) → BUG-229 (`start` serves stale `dist/` — **P1 the moment a deploy script exists**) → BUG-224's env reconciliation (`RESEND_API_KEY` / `EMAIL_FROM` / `PUBLIC_APP_URL`). Phase 0 (September 7) already found the requirements; BUG-219 → 223 are FIXED; two things the deploy did NOT settle, neither of them code: **`TRUST_PROXY_HOPS=1` is reasoned, not measured** (safe at default 0; do not set before the two-network curl), and **`/readyz`'s 2 s bound has STILL never met a real Neon cold start.** ✅ Hans's side — GCP project, the dedicated build SA and the first `gcloud run deploy` — **DONE September 12; DNS remains.**
3. ✅ **DONE — the INTRA-DISH measurement (read-only).** Verdict on D-WS9-239 / BUG-256: the lever is real (~12 min mean, shelf 6 → 17–20 at the bound) and it is a GENERATOR-CONTRACT question, not a scheduler-ordering one; existing text alone buys ~1 min.
4. ✅ **RULINGS DONE — D-WS9-238 and BUG-205 both ruled September 14; THE CORRECTNESS BLOCK IS CLOSED.** ✅ D-WS9-239 RULED (a): its build (generator contract + scheduler honours the tag + tagging pass, Phase 0 measures the pass's cost on 20 meals and hand-checks 20 tagged dishes) is **the next server block after WS9A lands, before the UAT supplement.**
5. ✅ **Deep Kiwi mark WIRED `f0b392c`, pushed, header + icon + splash verified on the dev build** (launcher icon: one look owed). ✅ D-WS9-239 Phase 0 done · ✅ BUG-260 done. 🔴 **chat-Claude's next prompts, in order: D-WS9-239 is 1a ✅ landed → 1b delivered (gate) → 1c the catalog pass (~$17) after the gate → 1d (text-mode generators + mobile carrier) after 1c, not launch-gating; ✅ the auth surface is RULED (D-WS9-241) as THREE lanes, in this order: BUG-235 client (mobile, NOW) → auth-core A+B+E (`routes/auth.ts` + `AuthContext` + `lib/auth.ts` + `index.tsx` + `sign-up.tsx`; needs the server slot, so after 1a lands) → error-copy D (six mobile surfaces).** **Then the redesign arc — RULED IN, Blocks 0–2 (D-WS9-240; roadmap row 2b)** — it does not start until the correctness block closes and D-WS9-238 is ruled (scope §5). 🔴 **The go-live IN list, in chat-Claude's recommended order (NOT Hans-ruled as an order): Tier 1 — EAS dev build (Android) · correctness close · BUG-235 · BUG-121 · spend caps · web probe · D-WS9-239 · arc Blocks 0–2. Tier 2, if the date holds at ~October 15 — WS9B · meal images · favorites/ratings · the 30-minute supplement.** Cross-workstream order per `kiwi_roadmap.md` (§8.1); the queue behind the block is unchanged — D-WS9-226 (intake + CSV load) · D-WS9-214 (c) once its per-meal cost is measured · D-WS7-213 (assign meals to days; check D-WS9-191's spec before building HALF 2 twice) · D-WS9-191 → the prep/plan-narration block (BUG-192 · 193 · D-WS9-198, ONE prompt surface) → the duplicate-meal block (BUG-162/166) → BUG-157 (unruled) → BUG-158–161 → BUG-133 → **BUG-121** (biggest pre-beta item; hand it D-WS9-178 AND -179) → **BUG-122** (hand it D-WS9-178) → the mobile fix pass → WS9 close → WS9A also owns D-WS9-181 (the test environment).

**Deferred or parked — Hans-ruled, do not re-open:** **BUG-254** (save screen leaves its loading state early) → polish, after the first go-live activities · **roadmap row 4** ("what to cook right now") → past go-live ("pretty sure") · **BUG-253** (Ask-Kiwi import → one dish → no parallelism) → the redesign arc · **BUG-250** (re-opening a saved plan option re-pays `expand`) → open, unassigned · **BUG-133 / 155** deliberately NOT started — they change what a list CONTAINS and would make the next grocery fix unattributable · **BUG-241 / BUG-242** parked behind the grocery narrowing, deliberately · 🔴 **the grocery fix-pass is STOPPED (chat-Claude, September 9) — the one genuine loop was the tap-outside enhancement, CUT after three regressions; everything else surfaced was pre-existing structure.** · **BUG-227 · 229 · 230** none commissioned (BUG-227 belongs in a Phase 1b, not a cleanup pass) · **D-WS9-228** (eggs: `1 dozen LARGE`, alias-precedence only — the `egg` / `Large eggs` `distinct` edge STANDS, no fold) UNBUILT, no block · **D-WS9-232 · 233 · 234** definition-track, none commissioned (D-WS9-233 RULED: per-dinner-serving macro targets that STEER generation, shown as asterisked daily averages, stored WITH scope; its launch-vs-after timing is unresolved · D-WS9-234 renames D-WS7-202's favorite and answers its blocking question — read it before WS7-11) · **BUG-231 / 232** → D-WS9-226's intake block / needs the category sweep first · **D-WS9-229** items 2 and 3 unbuilt (the env-gated dev escape must be impossible on a deployed instance; Hans's rate-limit copy — *"Kiwi seems to be missing the mark, please refine your preferences or use the 'Tell Kiwi' option…"* — is the durable part).

**Rulings from this block a fresh chat must not re-open:** **the cap is a CEILING, not a target** (D-WS7-166 — *"it's not saying 'anything under that cap is eligible' it's 'aim for meals that take as long as the cap' where it really should be the first version and honor the cap"*) · **an honest 38 beats a lying 45** (*"it's a problem when a recipe says 45 minutes and 1.25 hrs later you're eating"*) · **D-WS7-142:** a step-duration edit persists on EITHER save choice · **D-WS9-236:** hands-on time gets LABELS, not new cap options · **D-WS9-235:** meal time is DERIVED from steps with the scheduler's parallelism, active AND total captured, an unmeetable cap gets an HONEST NUMBER not an error page · **BUG-255 ruled (b)** and Hans's same-morning challenge (Kiwi CAN invent meals; what concentrates the repeat is D-WS9-073's shelf GUARD written for 1,214 meals and applied to six) is **D-WS9-238**, not a reversal · **the attendance hypothesis is REFUTED and withdrawn** (BUG-245: 12 minutes over-marked on the worst meal, zero on the second; 30 of 50 attended minutes are prep and assemble) · 🔴 **BUG-256's "~40% high / ~56–58" was chat-Claude's hand schedule with the finish-alignment constraint DROPPED (CC's ALT-C reproduces it exactly); under the rules as ruled the ordering effect is 1.35 min mean and the burger meal is 80 → 65. Do not re-quote 56–58.** · **Launch shape:** keep building as planned; will not ship unfinished to attract customers; an earlier store release with two-week resubmissions is possible — no action owed.

**Launch housekeeping (Hans, September 12):** company bank account ✅ OPEN · Apple Developer ✅ (Small Business Program enrollment awaiting approval) · Google Play Developer ✅ · Google Cloud account ✅ — **GCP DONE September 12 (chat-Claude in the built-in browser, Hans confirmed billing): project `kiwi-prod` (`kiwi-prod-508416`) under org `hans-tiefenthaler-org`, billing account "Kitchen Wizard" linked on the existing KITCHEN WIZARD, INC. payments profile, Cloud Run / Cloud Build / Artifact Registry / Secret Manager enabled, $25/month alerts-only budget — IDs on `kiwi_go_live_todos.md`. ✅ **the gcloud CLI is INSTALLED AND CONFIGURED (September 12): `account = hans@kitchenwizard.ai` · `project = kiwi-prod-508416` · `run/region = us-east4`, read back from `gcloud config list`. NOTHING on the account side is outstanding.** 🟢 **CLOUD RUN IS LIVE September 12: service `kiwi-api` in `us-east4` at `https://kiwi-api-166146829159.us-east4.run.app`, revision `kiwi-api-00002-bz5`; the four secrets in Secret Manager; build identity `kiwi-build@kiwi-prod-508416.iam.gserviceaccount.com` (`roles/run.builder`); runtime identity still the DEFAULT COMPUTE SA — a go-live row, logged, not today's work.** · **ToS + Privacy Policy: the September 7 draft is with the lawyer** · **Deep Kiwi mark: WIRED, PUSHED, header device-verified — the icon/splash half closes with the EAS build** · **EAS dev build: Hans is on ANDROID; IN for launch; ⚠️ gated on the ACCOUNT-IDENTITY row (Expo is on the personal Gmail) — steps owed from chat-Claude** · 🐛 **September 12 evening: Expo Go loaded BLANK on wifi — measured cause: the api-server was not running (nothing on :3000; IP unchanged at `.50`); one change (start it) fixed it. The cellular test was never valid: `EXPO_PUBLIC_API_BASE_URL` is a LAN address. The DEFECT underneath — no timeout / no failure state above `index.tsx`'s `isLoading` gate — is BUG-258, P2 but launch-shaped now that the API is public. Switching the app to the Cloud Run URL is the durable fix for the LAN class and is held until Hans rules on testing against production Neon.** · **mirror hygiene DONE by Hans September 12: `Claude outputs\` removed from `kiwi-canon`; the September 2 deferred-log archive (D-WS3–D-WS6) copied in — CC can grep those 140 entries again** · the 30-minute target list (95 dinners with dish split, CSV + README on Hans's disk at `artifacts\api-server\scripts\output\ws9-30min\`, gitignored) feeds the UAT catalog supplement — acceptance is the DERIVED time after generation, never the estimate. 🔵 **Chunky salsa is a 15th BUG-244 row awaiting Hans.** 🔵 **The chipotle apply landed (`created: 2`, total 1872).**

*Prior:* September 12, early ET — the attendance hypothesis refuted; BUG-256 minted; BUG-255 ruled (b) and challenged (→ D-WS9-238); the v9 ceiling wording built `aa13fc6`; the redesign arc scoped; `kiwi_cc_prompt_SERVER_scheduler_order_and_expand_v9.md` instructed (now done).

---

🔵 **PROCESS DEBT OWED AT THE NEXT CLOSE BATCH (working agreements; none written yet):** §27.4 — *"a hash check that is only run when you expect success is not a check"*: count anchor uniqueness BEFORE breaking, run the restore verification unconditionally (CC's own self-report) · §29.1 — the two-lane rule Hans approved (launch infra in parallel) is NOT written, so the September 7 lanes are not precedent; and a BREAK is visible cross-lane — a lane whose suite goes red on a file it does not own re-runs before diagnosing (the `// BREAKB` projection, September 10) · §11 — the device-script command is `cd artifacts/kiwi` then `pnpm exec expo start --clear` (`pnpm --filter @workspace/kiwi start --clear` has no `start` script; Hans worked around it silently for four scripts) · §27.6 — the suite-line rule: a suite figure is one run, never two transcribed as one (`1950 / 1947` was CC's admission) · §3.1 — verification in the SAME window after an apply returns · the GET-emits-by-`include` pattern (a column reaches the wire only because the query is unrestricted) becomes a standing hazard at its second instance — the meal serialiser is the next candidate · §6.1 / hazards — `grep -a` for the two NUL-byte files · **NEW September 12 — §16.2 / §16.3: a mirror sync must NEVER run ahead of a REFUSED canon write; check the write result (and knowledge size) before syncing, or the mirror asserts rulings canon does not hold** · **NEW September 12 — §26.5's backward sweep: ~80 chat-only prompts/scripts/handoffs were sitting INSIDE the greppable mirror under `Claude outputs\` — removed by Hans the same day; the rule that they never land there needs its line** · ✅ the September 2 deferred-log archive is now in the mirror (Hans, September 12) · **NEW September 12 — the root `tsconfig.json` references a `lib/db` that does not exist (that is the TS5083); fix on next touch, not mid-lane** · **NEW September 12 — `.prisma/client/` on Hans's disk holds ~1.2 GB of `query_engine-windows.dll.node.tmp*` leftovers from the EPERM regenerate loop — local hygiene, delete when the dev server is stopped** · **NEW September 12 — §27: chat-Claude verified the probe FLAG SYNTAX against Google's reference and passed CC's probe VALUES through unchecked. `--startup-probe timeoutSeconds=3,periodSeconds=2` is rejected outright (`timeout_seconds must be less than period_seconds`) and cost Hans a failed command at the deploy. CHECKING THAT AN ARGUMENT EXISTS IS NOT CHECKING THAT ITS VALUE IS LEGAL — and a constraint BETWEEN two values is exactly the class a reference page will not flag for you.** · **NEW September 12 — §27.4: a restore check of "`git diff` empty on that path" is UNFIT for a file the commit itself modifies (the mark lane: `HomeHeader.tsx` was legitimately changed vs HEAD, so the diff could never be empty). The check is a HASH before the break and after the restore. CC caught the flaw in the instruction; chat-Claude wrote it.** · **NEW September 12 — §26.5 / §2: `git status` in the REPO shows an untracked `Claude outputs/` directory at the Kiwi-App root — not the mirror, the repo. It is NOT gitignored; a stray `git add -A` would commit chat-only artifacts. Hans: move it out or add it to `.gitignore`.** · **NEW September 12 — §16.3: a mirror sync from chat-Claude wrote a canon body WITHOUT its banner once (the bug log); caught on the size check and re-stamped. The banner is 1,396 bytes and the mirror copy MUST be canon body + banner, never canon body alone.** · **NEW September 13 — §11: EVERY command block in a device script carries its own `cd`. The EAS script's step 5 relied on the folder from step 4; Hans ran it from the repo root, and `eas build` generated a stray `eas.json` there before failing. A device script is executed one block at a time, often in a fresh window — no block may inherit a working directory.** · 🔴 **NEW September 13 — §11, AND IT IS CHAT-CLAUDE'S SECOND ERROR OF THE SAME CLASS IN THREE DAYS: AN UNQUOTED COMMA IN A POWERSHELL ARGUMENT BECOMES AN ARRAY, WHICH POWERSHELL JOINS WITH A SPACE BEFORE HANDING THE NATIVE COMMAND ONE ARGUMENT.** `--update-env-vars=AI_USER_DAILY_CALLS=500,AI_DAILY_CEILING_USD=10` reached gcloud as a single var whose VALUE was `500 AI_DAILY_CEILING_USD=10`; the second variable was never set and the first parsed to `NaN`. **The spend guard was fully inert and the command reported success.** **TWO DISTINCT MECHANISMS, BOTH HIT WITHIN THE HOUR, AND QUOTING ONLY FIXES THE FIRST:** **(1) an unquoted comma becomes a PowerShell array, joined with a space — fix: quote the argument, `"--update-env-vars=a=1,b=2"` (verified working). (2) `gcloud` on Windows is a `.cmd` SHIM, so arguments cross PowerShell *and* `cmd.exe`, and EMBEDDED double quotes are stripped in transit — `'…jsonPayload.msg:"spend guard"'` reached gcloud as `…:spend guard` and `guard` became a stray positional (`ERROR: unrecognized arguments: guard`). Quoting the outer string does NOT save this.** 🔴 **THE RULE, CORRECTED: never put a SPACE inside a gcloud filter/query argument on Windows. Filter on a single token (`jsonPayload.event=spend_guard_refused`), or drop the filter and narrow with `--limit` / `--freshness` instead.** ⚠️ **The first version of this very line said "quote it" and was WRONG — it would have sent the next reader into mechanism (2).** ⚠️ **The sibling error was the probe values on September 12 (`timeoutSeconds` vs `periodSeconds`): both times the FLAG was verified against the vendor's reference and the thing AROUND the flag — the shell in one case, the relationship between two values in the other — was not.** ✅ **And the system caught it exactly as designed: BUG-263's boot validation existed to make a set-but-unparseable var loud, and it fired on its first real use hours after landing.** ⚠️ **Second lesson, equally practical: `gcloud run services logs read` shows REQUEST LINES ONLY — it does not render the `jsonPayload` pino writes, so the boot line is invisible through it. The command that reads structured logs is `gcloud logging read 'resource.labels.service_name="kiwi-api" AND jsonPayload.msg:"…"'`. A diagnostic nobody can read is not a diagnostic.** · 🔴 **NEW September 13 — PINO RESERVES `name`. A structured log field called `name` is consumed as the LOGGER name and rendered into the line prefix instead of appearing in the object** (found by CC smoke-testing the BUG-263 boot line; renamed to `envVar`). **Every `logger.*` call in this codebase that passes a `name` key is silently losing it — worth one grep at the next logging touch.** · **NEW September 13 — the secret-hygiene rule extends to LOGS, and now has a test: `da8969b` asserts that `DATABASE_URL` / `JWT_SECRET` values present in the environment never appear in the boot summary or the invalid-var error. §3.1 r13 was written about the repo tree; a log line is the other way a secret escapes.** · the stray `kiwi-canon\kiwi_working_agreements-1.md` (842 bytes) is junk. · 🔴 **NEW September 13 — §2 / §26.5, THE SAME HAZARD AS `Claude outputs/` AND WIDER THAN ANYONE NOTICED: `git status --short` at the root lists EIGHTEEN untracked `artifacts/api-server/scripts/<probe-folder>/` directories** (`ws9-239`, `ws9-bug245`, `ws9-sched-order`, `set-test-password`, `probe-grocery-titles`, … every read-only lane this arc has run). **`scripts/output/` is gitignored; the probe SOURCE folders are not.** A stray `git add -A` commits all eighteen — and `set-test-password/` is the one whose NAME alone says do not commit it. ✅ **Not urgent** (every lane stages by explicit path, and `9b9b01a` closed the root-level case), **but the next lane that touches root `.gitignore` should add a pattern for probe folders, and chat-Claude should stop telling lanes to create them at a tracked path.** · 🔴 **NEW September 13 — §27.6: A RULING WAS MADE ON AN UPPER BOUND RELAYED AS THE EXPECTATION.** D-WS9-239's *"~12 min mean, shelf 6 → 17–20"* was the every-attended-step-overlaps ceiling from the September 12 measurement; the text-recoverable figure in the same report was 1.22 min. Chat-Claude wrote the ceiling into the entry as the lever, Hans ruled on it, and the Phase 0 prompt then called it "the regex estimate". Real tags: 5–8 min. **THE CHECK: when a measurement reports a bound AND a point estimate, the entry names which one it is quoting — and a ruling never cites a bound as an expectation.** · **NEW September 13 — §16.3: `device_commit_files` with TWO large files in one call truncated BOTH (636 KB → 617 KB; 997 KB → 992 KB); one file per call landed exact on the first try. Commit mirror files ONE PER CALL and size-check each.** · 🔴 **NEW September 13 — §8.1 / §27: THE GITHUB-SYNCED SOURCE IN PROJECT KNOWLEDGE IS THE FROZEN `main` BRANCH, NOT THE WORKING TREE.** `project_search` returned a `package.json` listing `@clerk/expo`; chat-Claude relayed it into a prompt as a fact about `ws7-in-progress`; CC found it absent. **A synced file establishes what `main` held at the last sync — never existence in the working tree. Existence checks go through the device bridge or CC, not the sync.** · **NEW September 13 — §16.3, a THIRD shape of the mirror-commit hazard: the FIRST `device_commit_files` after REBUILDING a large mirror file wrote the PREVIOUS bytes (mtime moved, size did not — 1,003,297 stayed 1,003,297); the second identical call landed 1,012,541. Commit, `device_list_dir`, and if the size did not move commit AGAIN — a moved mtime is not a landed write.** · **NEW September 13 — §25 / §8: PRD §3.8 says a bootstrap network error shows Welcome "for now" and does not clear the token; D-WS9-241 (B) rules a failure screen with the token kept. Redline at close, not a re-ruling.** · 🔴 **NEW September 14 — §6.1's FAMILY GAINS A FOURTH MEMBER, AND IT IS IN THE TOOL RATHER THAN THE FILE: CC reports that this box's Bash heredocs COLLAPSE `\\` TO `\`, so several `perl`/`node` one-shots in the 1b-ii lane matched nothing and changed nothing — silently, exactly like the CRLF no-op in §27.5.** ✅ **CC caught it itself and verified every landed edit by CHAR CODES or by tests reading the evaluated bodies.** **The rule to carry: an escape-bearing one-shot inside a heredoc is not trustworthy — use the Edit tool, or verify the mutation by reading the bytes back, never by the command's exit status.** · 🔴 **NEW September 14 — §11 / §27, AND IT IS THE THIRD INSTANCE OF ONE SHAPE: A DEVICE STEP WRITTEN FROM THE RULING INSTEAD OF FROM THE CODE.** Two in one script: `adb shell am start …` was handed to Hans without ever checking `adb` is on his machine (it is not), and `555123` was chosen as a "deliberately bad phone" without reading the rule, which is *at least 7 digits* — so the step tested nothing it claimed to and Hans reasonably adjusted until it saved. **The sibling is the Cloud Run probe values (September 12) and the PowerShell comma (September 13).** ✅ **THE CHECK, and it is cheap: before a device step names a TOOL, confirm it exists on Hans's machine; before it names a VALUE, read the rule that will judge it. A device script is executable documentation — every literal in it is a claim about the system.** ✅ **Both were caught the same morning by measuring through the bridge rather than theorising, which is the habit that keeps working.** · 🔴 **NEW September 14 (evening) — §27.6 CLARIFIED, AFTER A LANE WALKED THROUGH AN EXPLICIT STOP ON ITS AUTHORITY: §27.6 IS A LICENCE TO REFUSE AND TO STOP — NEVER A LICENCE TO CONTINUE PAST A STOP.** The supplement lane hit *"fewer than 5 of the first 10 ≤ 30 → STOP"* with 4, and continued because the gate’s stated spend rationale (~$40 remaining) was false (~$4). **The rationale being wrong does not unlock the stop: stopping is the conservative action by construction, and a wrong figure in the rationale is REPORTED at the stop, not used to walk through it.** Its own report conceded the gate’s diagnosis was right and the override cost $4.36 and 87 non-target catalog rows. **Refusing to ACT and refusing to STOP are opposites; only the first is §27.6.** · ⚠️ **NEW September 14 — `plans.test.ts` *"returns 429 when the mutation rate limit is exhausted"* is a TIMING FLAKE under full-suite load** (token-bucket refill; 3/3 green in isolation, green on the next two full runs). Pre-existing. **A lane that sees exactly one red on that test re-runs before diagnosing.**

🔵 **CARRIED, NO ID:** ✅ **BUG-101 / BUG-104 RUN DOWN September 13 — the bug log was stale, this block was right.** BUG-104 is FIXED (`83fb435` + `2aae661`, device-tested) and carried TWO `Status:` fields, the stale OPEN one first — removed. BUG-101 was downgraded P1 → P3 at its Phase 0 (an all-time rate over eight prompt versions; the active v8 has zero failures) and routed to the latency program — the status field never took it; corrected. **The live P1 list is therefore: BUG-205 (Hans's ruling, correctness block) · BUG-121 (queued) · BUG-235 (password-reset client, beta blocker) · BUG-224 (in progress, Hans's DNS).** · ⚠️ **The device bridge injects C2PA content-credential metadata into every image it writes (+5,770 bytes per PNG; an inline `<metadata>` block in an SVG). Files stay valid; byte sizes will never match the generator's. Any byte-compare across the bridge must strip it first.** · neither the session-expiry cascade nor `logout()` clears the non-auth caches (plans, meals, home) — CC declined to fix one-sidedly and was right; **a second user signing in on the same device could see the first user's cached rows until a refetch — UNVERIFIED, needs two accounts before it is called a leak** · `sweepExpiredUsedTokens` exists and nothing calls it — go-live list · two concurrent redemptions of one token against live Neon would close BUG-233's last unverified claim — cheap, unrun · the React Compiler SILENTLY skips any component containing `try/finally` — go-live list · **BUG-224's remainder:** DNS verify at GoDaddy (Hans) · the D-WS9-231 page (BOTH routes) · BUG-235's client · `EMAIL_REVIEW_RECIPIENT` is documented and read by nothing · the Resend `403` proves the provider was REACHED, not that the body is right — **if a 403 survives verification, check the KEY'S SCOPE before the code; do not ship reset from an unverified domain, and a verified domain sending links to a page that 404s is worse** · **a separate process reports its own last-known state as current** (`4721d58`'s report told Hans a migration was unapplied that he had already applied) — check any instruction in a CC report against what happened OUTSIDE that lane before relaying it · `pnpm typecheck` at the WORKSPACE ROOT fails on TS5083 (`lib/db/tsconfig.json`) — pre-existing; do not chase it mid-block; `tsc` alone is not sufficient (it passed a broken template literal `esbuild` caught) · `pnpm dev` refuses to boot with `RESEND_API_KEY` set unless `EMAIL_FROM` and `PUBLIC_APP_URL` are also in `.env` (both placeholders in Hans's `.env` today) · 🔴 **`artifacts/api-server/DEPLOY.md` still documents probe values Cloud Run REJECTS (`periodSeconds=2` with `timeoutSeconds=3`); the live service runs `periodSeconds=4` / `timeoutSeconds=3`. Correct it on the next touch of that file — as written it is a copy-paste trap for the next deploy.**

🔴 **STANDING SCOPE RULE — D-WS9-230, Hans-ruled September 7, 2026: NO BACKFILLS. Fixes are FORWARD-ONLY.** ⚠️ **CLARIFIED September 9 after Hans asked where the rule came from — it was HIS, and it was about not repairing HIS OWN TEST RESIDUE (plans and lists he generated while testing). The carve-out for SHARED PRODUCT DATA was in the ruling from the start, and meal cook-times on catalog meals sit inside it. His September 9 *"I'm open to data backfills/supplementing/whatever needs to happen to get things right"* is CONSISTENT with the rule as written, not a reversal: shared data may be repaired; his broken test plans still need not be.** *"there is NO NEED to backfill or fix data. I can test on new plans for everything… bad data is fine for me to ignore."* **Existing plans, lists, meals and imports are NOT repaired, migrated or audited for residue; acceptance runs on a FRESH plan.** ⚠️ **This project has repeatedly built a second thing to repair what a bug already wrote — apply the ruling aggressively.** 🔴 **CARVE-OUT, NOT OPTIONAL: the SHARED INGREDIENT CATALOG is product data every new user inherits and is NOT covered — D-WS9-226's CSV load, pack sizes and the relationship program stay in scope. The test is "would a user signing up tomorrow inherit this row?"** ⚠️ **And it does NOT license shipping unverified — the acceptance test moves to new data, it does not disappear.** Edits to a base dish do NOT propagate to a dish already in a plan — forward only.

🔴 **AMENDED September 14, 2026 — THE RULE NOW REACHES THE SCHEDULE, NOT ONLY THE DATA.** Hans, verbatim: *"this is development mode until we actually go on an app store… it's ok for the app to be unusable for a couple days at a time if necessary"* — **if a broken intermediate state buys a faster route through the roadmap, take it.** ✅ **What this licenses:** a rewrite REPLACES a flow instead of running beside it (no feature flag, no dual path, no compatibility shim built only to keep the old one alive) · a lane may land knowingly broken and be fixed by the next one · **a sequence is split by what is efficient to BUILD, not by what keeps the app usable** · device testing goes at the END of a sequence · a schema change may reshape a user-scoped column with no migration path for its rows. 🔴 **What still binds, and the line is REVERSIBLE vs EXPENSIVE-TO-UNDO, not user-visible:** the shared ingredient catalog and the 1,872-meal catalog (the carve-out above) · anything unrecoverable (the Play Store application id, a published build, a live sending domain, a dropped column holding catalog content) · **the audit gate (§3) and acceptance — speed comes from sequencing, never from skipping the check.** ⚠️ **A MEASUREMENT GATE IS NOT WHAT THIS RELAXES:** D-WS9-239's fresh-20 gate protects the CATALOG (1c writes tags onto shared meals), not Hans's comfort. **Read a gate's purpose before citing this at it.** ✅ **And it retires a class of paragraph: "note that plans generated between X and Y may read wrong" — Hans does not need to be warned about his own scratch data.**

⚠️ **STANDING HAZARDS — pointers only; read the entry before touching the named code:** 🔴 **`Ingredient.aliases` IS A DEAD COLUMN — superseded by the `ingredient_aliases` TABLE.** Writing to the array looks done and resolves nothing. · 🔴 **A CATALOG MERGE HAS TEN CARRIERS, NOT THREE, AND `RecipeInstructionStep.amountRefs[]` HAS NO FOREIGN KEY** — deleting a row with live `amountRefs` leaves no error, just cook steps that silently render wrong. Use `src/lib/ingredientMergeCarriers`. · ⚠️ **GROCERY GENERATION IS NOT READ-ONLY — `fillPurchaseSizesWithWriteBack` WRITES PURCHASE PACKS BACK TO THE `Ingredient` CATALOG** (BUG-124's machine). Any "generate and throw away" procedure must measure the write-back misses first. · 🔴 **ALLERGEN STAMPS ARE NAME-DERIVED AND THE FALSE-POSITIVE CLASS RECURS (D-WS9-211)** — food naming is compositional over an open vocabulary, and **`Ingredient.category` is NOT a usable cross-check** (`vegan butter` is filed as Dairy). Do not propose it again. · ⚠️ **A `dev` server is only as fresh as its last restart, and the DB serves OLD prompt bodies until `prisma:seed` runs** — any block that edits a prompt body owes a reseed step in the device sequence (§6, §7). · ⚠️ **Expo Go tracks only the newest SDK and auto-updates; the SDK-57-vs-54 break recurs at every Expo release.** The durable fix is a development build — pulled-forward critical path for friends-and-family beta. Do NOT jump SDK 54 → 57 to unblock a test (D-WS5-034 owns SDK updates). · ⚠️ **`prisma migrate deploy` is DENIED to CC by the permission classifier — Hans runs migrations (§2).** · 🔴 **A SCREEN'S HOOKS MUST SIT ABOVE ITS EARLY RETURNS — `app/**` is outside the test glob and the workspace has no ESLint react-hooks plugin, so only a device catches a violation (`e644c6c`); every prompt that adds a hook to a screen says so.** · 🔴 **`rg` AND PLAIN `grep` CALL `groceryMerge.ts` AND `ingredientRelations.ts` "BINARY" — ROOT CAUSE FOUND September 11: each holds a literal NUL byte in source (a `\0` pair-key separator inside a template string, `groceryMerge.ts` ~:288 and `ingredientRelations.ts` ~:55). `grep -a` reads them; `rg -a` likewise. Not corruption. Fix on next touch: replace the separator with a printable one or a `\u0000` escape. Until then every search over `src/lib/grocery*` uses `-a`.** · ⚠️ **`kiwi_prompt_update_playbook.md` says "stage, don't commit — Hans commits"; §2 says CC makes local commits. The playbook is stale on that line — fix at the next prompt-playbook touch; prompts say "commit by explicit path" and win.** · ⚠️ **`HAND_MAP_VARIANTS` (the disjointness guard's copy of the hand map) lists 11 entries; the map has 16 since A3 added five garlic spellings — the guard does not cover garlic. Fix on next touch of `ingredientRelations.ts`.** · 🔴 **NEVER PUT A PATHSPEC-TAKING GIT COMMAND INSIDE A COMPOUND COMMAND — a server lane ran a bare `git checkout --` that way on September 10 with a mobile lane's edits uncommitted (a no-op by luck; `git checkout -- .` would have destroyed them), and the NEXT lane did it again despite the warning in its prompt — the permission classifier blocked it. CC reaches for it to restore a deliberate break; restores belong to the Edit tool or a standalone `git restore -- <exact path>`.** · 🔴 **`device_commit_files` REPORTS SUCCESS AND WRITES NOTHING ON LARGE FILES — every canon sync re-commits with `force`, then `device_list_dir` and checks the byte size, then reads the banner back. It failed twice on September 12 alone.** · ⚠️ **A Python edit script that asserts mid-run leaves later file writes unrun — re-apply the tail separately.** · **D-WS9-189** (do not fix another fragmentation instance one-off; it belongs to the program) · **D-WS9-204** (the substitution MECHANISM must NOT be built by overloading an `IngredientRelation` label) · **D-WS9-147** (`isActiveThisWeek` is derived) · **D-WS9-178** (as-purchased macro convention) · **BUG-080** (gates WS7-11) · **BUG-067 / BUG-173** (the same composer drops `displayTitle`) · **D-WS9-152** (the Zod + inline-`select` correctness pass; has lost its budget twice) · **D-WS9-125 / D-WS9-127** (ABANDONMENTS, not deferrals) · **BUG-106** (`neutral[600]` measures 3.49:1, app-wide AA shortfall) · **BUG-202** (`onSageSub` 3.7107:1, same family) · **BUG-123** (BUG-096's 81-pair claim is bounded to suffix-fold collisions) · **BUG-188** owns cilantro-as-three-lines (`ceil` a measured herb need to whole bunches) — **it is NOT a conversion-figure problem; chat-Claude has written that wrong verdict twice. Do not write it a third time.** · 🔴 **A GUARD BOUNDARY MUST NOT SIT WHERE THE MODEL'S NOISE LIVES (BUG-214, measured September 9): the model varies its output at temperature 0 always, and that variance becomes user-visible the moment a guard's accept/refuse decision straddles it. An UNCONDITIONAL refusal hides it; a CONDITIONAL one exposes it. Read BUG-214 before designing any guard that can go either way.** · 🔵 **Carried, no ID: `LLMCallLog` writes FAIL SOFT and silently drop for an unknown `userId` — surfaced by a probe's fake user, so not a production trigger, but worth a look in any AI-cost audit (D-WS6-030).** · ⚠️ **STALE CODE COMMENTS, fix on next touch — verified still present September 7 in the working tree:** `groceryStaples.ts` (the `BUG-170 / BUG-168` variant-fold comment should read BUG-172; and the `STAPLE_VARIANT_TO_BASE` doc comment still says the fold runs "BEFORE the membership check" — BUG-182 removed it) · `groceryMerge.test.ts` (two `BUG-170` comments → BUG-172). ⚠️ **Both files were edited in A2b/A3 and the fix-on-next-touch rule was missed — name it in the next prompt that opens them.**

🔴 **CANON-INTEGRITY, September 8, 2026:** a parallel chat overwrote `kiwi_bug_log.md` from a stale base and destroyed a day of edits; caught by §29.2's fresh-pull and re-merged by hand. **HANS RULED: NO WORKING-AGREEMENTS AMENDMENT — *"this was a one-off accident that caused some rework."* Do not re-propose one.** His practice is the control: he sometimes returns to a PRIOR chat for its last 5–10% of context and tells that chat not to write canon. **A chat told to stop writing canon is the fix, not a new rule.**

⚠️ **COUNTERS ARE DELIBERATELY NOT STATED HERE (§A(1)).** Take them from each log by **`max(heading-grep, pointer line)`** — ⚠️ heading-grep in the LIVE bug log no longer finds ✅ IDs below BUG-225 (archived to the mirror); the counter is unaffected because the newest IDs are live. ⚠️ **BUG STATUS IS NOT STATED HERE EITHER** — a status claim in this block went ten days stale on BUG-171 and was inherited by a CC prompt. Read the bug log.

---

⚠️ **THIS BLOCK IS REWRITTEN SHORT AT EVERY CLOSE — August 5 and 13, September 2, 4, 7 and 12, 2026.** Narration from earlier compactions lives in `kiwi_remediation_progress_ARCHIVE_2026-08-05.md`, `..._2026-08-13.md` and `..._2026-09-04.md` (the last also holds §3–§8's 85 KB of WS1–WS6 build record; all Hans-authorized; **not project knowledge**; the September 4 archive is in the mirror per §24.10). **The September 12 rewrite archived nothing verbatim: every line it dropped stated of itself that it was recorded on a named BUG / D-WS entry, or was a dated "prior position" paragraph — the doc's own rule says neither belongs here.** ⚠️ **THE REASON THIS BLOCK MUST STAY SHORT IS MECHANICAL: this doc returns INLINE from a read, so every position update is a full rewrite.** ⚠️ **LENGTH WAS THE SYMPTOM; THE FAILURE WAS CONTRADICTION** — the August block held three answers to "what is next". **One current state, one prior line. Close narration belongs in the entry's own resolution text.**

